# Data 360 — August 10, 2026

**Window:** 2026-08-09 03:55 → 2026-08-10 03:36 UTC (24h, extended to 72h) · **Checked:** github.com page fetches, PyPI, npm, web search · **Not reachable:** `help.salesforce.com`, `developer.salesforce.com` (EGRESS_BLOCKED, so the Data 360 release notes went unread this run)

## What changed

Nothing cleared the relevance bar. No Data 360 product announcement, connector release, release-note change or repository commit landed in the 24h window, and the 72h extension surfaced only the `sfsqlquery` and `sf-data360` items the 08-09 scan already recorded.

## Verified negatives

- `salesforce-datacloud-connector` (PyPI) — still **2.0.0b1** and still the only release; **beta since 2026-06-02**, v1 `salesforce-cdp-connector` still deprecated, checked 03:38 UTC
- `salesforce/sf-pi` — `sf-data360` extension unchanged across v0.261.0–v0.262.1; the three releases in the window touch `sf-welcome`, `sf-slack` and `sf-environment` only, checked 03:40 UTC
- `salesforce/next-gen-wealth` (Agentic Advisor) — no change surfaced in the window, checked 03:46 UTC
- Data 360 monthly release notes — no new monthly section reachable; `help.salesforce.com` returned **EGRESS_BLOCKED**, checked 03:50 UTC

## Watched, not recorded

- **Currency Reporting** and **Result Reuse for Data 360 Live** — a sixth scan with no obtainable technical detail; still tracked in the [README](../README.md#open-questions-to-chase)
- Winter '27 secondary coverage names *"select writeback operation types for Data 360 objects"* and *"assign data model object categories for Data 360 compatibility"* — feature titles only, no surface, no identifiers; nothing to write until one has an API or a Setup path

## Follow-up on the 08-09 sourcing finding

The lesson held its shape but paid nothing today: the four unexamined `sf-pi` siblings (`sf-soql`, `sf-data-explorer`, `sf-brain`, and `sf-guardrail` beyond its command gate) were checked for movement in the window and had none. They remain the best standing lead for Data 360 surface that ships without an announcement.

# 05-release-radar — Salesforce AI / Agentforce / Data 360 updates

**What this is:** a running log of Salesforce technical updates worth knowing as an AI-Salesforce architect. Fed by the `daily-agentforce-updates` scheduled task, which writes to the contract in [Writing contract](#writing-contract) below.

**How it's organized — two layers, one of them primary.**

- **Topic files** sit at the root of this folder and are **the layer to learn from**. Each holds the *running story* of one subject — dated entries newest-first, so a topic reads as a narrative over time. Every entry carries **What changed / Why it matters / Gotchas / Study action / Status / Sources**. They stay at the root deliberately: most cut across areas (developer tooling covers both Agentforce and Data 360; pricing covers both), so filing them under one area would lose information.
- **Dated scan notes** live in three **area folders** — [`01-agentforce/`](01-agentforce/), [`02-data-cloud/`](02-data-cloud/), [`03-salesforce-ai-research/`](03-salesforce-ai-research/). One `YYYY-MM-DD.md` per area per scan, and they are **audit logs, not write-ups**: what window was checked, what changed and which topic file it went into, and named verified negatives. A quiet day should read as a quiet day, not as a missed scan.

**How to read it:** skim this README for the current state of play → open the topic file for the subject you care about → use the dated notes only when you need to know *when* something was recorded or what a scan checked.

> Notes dated 2026-07-26 → 2026-07-30 predate this contract and are full write-ups. They are left as-is; the routine rewrites entries into the contract shape as it touches them.

## Areas — dated scan notes

| Folder | Covers | Latest |
|---|---|---|
| [01-agentforce/](01-agentforce/) | Agentforce platform, builder and Agent Script, developer tooling, governance, pricing | [2026-08-25](01-agentforce/2026-08-25.md) · [08-22](01-agentforce/2026-08-22.md) · [08-21](01-agentforce/2026-08-21.md) · [08-20](01-agentforce/2026-08-20.md) · [08-19](01-agentforce/2026-08-19.md) · [08-17](01-agentforce/2026-08-17.md) · [08-16](01-agentforce/2026-08-16.md) · [08-14](01-agentforce/2026-08-14.md) · [08-13](01-agentforce/2026-08-13.md) · [08-12](01-agentforce/2026-08-12.md) · [08-11](01-agentforce/2026-08-11.md) · [08-10](01-agentforce/2026-08-10.md) · [08-09](01-agentforce/2026-08-09.md) · [08-08](01-agentforce/2026-08-08.md) · [08-07](01-agentforce/2026-08-07.md) · [08-06](01-agentforce/2026-08-06.md) · [08-05](01-agentforce/2026-08-05.md) · [08-03](01-agentforce/2026-08-03.md) · [08-02](01-agentforce/2026-08-02.md) · [08-01](01-agentforce/2026-08-01.md) · [07-31](01-agentforce/2026-07-31.md) · [07-30](01-agentforce/2026-07-30.md) · [07-29](01-agentforce/2026-07-29.md) · [07-28](01-agentforce/2026-07-28.md) · [07-27](01-agentforce/2026-07-27.md) · [07-26](01-agentforce/2026-07-26.md) |
| [02-data-cloud/](02-data-cloud/) | Data 360 (ex-Data Cloud): ingestion, modeling, grounding, zero-copy, semantic layer | [2026-08-25](02-data-cloud/2026-08-25.md) · [08-22](02-data-cloud/2026-08-22.md) · [08-21](02-data-cloud/2026-08-21.md) · [08-20](02-data-cloud/2026-08-20.md) · [08-19](02-data-cloud/2026-08-19.md) · [08-17](02-data-cloud/2026-08-17.md) · [08-16](02-data-cloud/2026-08-16.md) · [08-14](02-data-cloud/2026-08-14.md) · [08-13](02-data-cloud/2026-08-13.md) · [08-12](02-data-cloud/2026-08-12.md) · [08-11](02-data-cloud/2026-08-11.md) · [08-10](02-data-cloud/2026-08-10.md) · [08-09](02-data-cloud/2026-08-09.md) · [08-08](02-data-cloud/2026-08-08.md) · [08-07](02-data-cloud/2026-08-07.md) · [08-06](02-data-cloud/2026-08-06.md) · [08-05](02-data-cloud/2026-08-05.md) · [08-03](02-data-cloud/2026-08-03.md) · [08-02](02-data-cloud/2026-08-02.md) · [08-01](02-data-cloud/2026-08-01.md) · [07-31](02-data-cloud/2026-07-31.md) · [07-30](02-data-cloud/2026-07-30.md) · [07-29](02-data-cloud/2026-07-29.md) · [07-28](02-data-cloud/2026-07-28.md) · [07-27](02-data-cloud/2026-07-27.md) · [07-26](02-data-cloud/2026-07-26.md) |
| [03-salesforce-ai-research/](03-salesforce-ai-research/) | Salesforce AI Research: benchmarks (CRMArena, SCUBA, GIFT-Eval), open models, agent-lifecycle tooling | [2026-08-25](03-salesforce-ai-research/2026-08-25.md) · [08-22](03-salesforce-ai-research/2026-08-22.md) · [08-21](03-salesforce-ai-research/2026-08-21.md) · [08-20](03-salesforce-ai-research/2026-08-20.md) · [08-19](03-salesforce-ai-research/2026-08-19.md) · [08-17](03-salesforce-ai-research/2026-08-17.md) · [08-16](03-salesforce-ai-research/2026-08-16.md) · [08-14](03-salesforce-ai-research/2026-08-14.md) · [08-13](03-salesforce-ai-research/2026-08-13.md) · [08-12](03-salesforce-ai-research/2026-08-12.md) · [08-11](03-salesforce-ai-research/2026-08-11.md) · [08-10](03-salesforce-ai-research/2026-08-10.md) · [08-09](03-salesforce-ai-research/2026-08-09.md) · [08-08](03-salesforce-ai-research/2026-08-08.md) · [08-07](03-salesforce-ai-research/2026-08-07.md) · [08-06](03-salesforce-ai-research/2026-08-06.md) · [08-05](03-salesforce-ai-research/2026-08-05.md) · [08-03](03-salesforce-ai-research/2026-08-03.md) · [08-02](03-salesforce-ai-research/2026-08-02.md) · [08-01](03-salesforce-ai-research/2026-08-01.md) · [07-31](03-salesforce-ai-research/2026-07-31.md) · [07-30](03-salesforce-ai-research/2026-07-30.md) · [07-29](03-salesforce-ai-research/2026-07-29.md) · [07-28](03-salesforce-ai-research/2026-07-28.md) · [07-27](03-salesforce-ai-research/2026-07-27.md) · [07-26](03-salesforce-ai-research/2026-07-26.md) |

## Topic files — the running story

| File | Covers |
|---|---|
| [agentforce-platform.md](agentforce-platform.md) | Agentforce Builder, Agent Script, Multi-Agent Orchestration, Voice, Contact Center, mobile SDK, observability |
| [data-360.md](data-360.md) | Data 360 (ex-Data Cloud): SOQL changes, Code Extension, Intelligent Context, semantic layer, zero-copy |
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | Hosted MCP servers, Headless 360, **MuleSoft Agent Fabric**, **ADLC workflow**, Apex/LWC changes, CLI, Agentforce Vibes, Agent Skills |
| [trust-security-and-governance.md](trust-security-and-governance.md) | Einstein Trust Layer, user-mode defaults, SOAP login retirement, secrets handling |
| [pricing-and-certification.md](pricing-and-certification.md) | Flex Credits, pay-per-resolution, Agentforce Specialist exam changes |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | Salesforce AI Research: CRMArena, GIFT-Eval, AnchorBench, MFCL Audio, LoCoBench-Agent, EDR, and the licences on them |

## State of play — as of 2026-08-25

Five things define the current Salesforce AI landscape. If you only retain five, retain these.

0. **Before any of the below: your tooling's patch level is not what you think it is.** `@salesforce/cli` publishes an **`npm-shrinkwrap.json`**, so every caret range in every plugin manifest is overridden at install time and **"I upgraded" answers nothing**. `sf` `latest` **2.148.3** carries SDR **13.0.1** (zip-slip fixed) and **not 13.1.1** (TOCTOU symlink escape, still `latest-rc` only), and `@salesforce/agents` **2.0.1** — one of the four `--api-name` preview fixes, not four. Three weeks of this radar's reachability arithmetic was wrong because it read manifests instead of the lockfile. **Read the shrinkwrap; the manifest is intent, the lockfile is fact.** ([entry](developer-tooling-and-apis.md#2026-08-25--sf-publishes-an-npm-shrinkwrapjson--the-clis-dependency-tree-is-pinned-exactly-and-three-of-this-radars-reachability-calls-were-wrong))
1. **The season turned: Winter '27 release notes are live, and this week is the migration window's hard edge.** Sandbox refresh cutoff **Aug 27, 18:00 PT — two days out**; preview instances upgrade **Aug 28–29**; production weekends **Aug 29 / Oct 3 / Oct 10**. **Two** Release Updates will break things quietly, and they fail differently: the **retirement of the OAuth 2.0 username-password flow** — every script posting `grant_type=password` stops getting a token on its org's upgrade date — and the **end of support for incorrect instanced URLs in API traffic**, which is not a code pattern but a *value* that went stale when Salesforce moved your org between instances, enforced **rolling** rather than on a date. Winter '27 is **API 68.0**. Summer '26 remains the current GA release.
2. **Headless 360 is still the organizing theme — and 08-16 established that the hosted server is a *dispatcher*, not a tool catalogue.** Every major capability is reachable as an API, an MCP tool or a CLI command; hosted MCP servers are GA. The `headless-360` server exposes **four meta-tools** — `discover`, `describe`, `dispatch_readonly`, `dispatch` — and `dispatch` takes **raw HTTP** over the Connect/Setup surface, with the org bound to the **session's OAuth JWT** rather than named in the call. That retires the "~100 admin-facing skills" framing this radar carried since 07-26: the answer to "is this operation supported?" is the REST route, not a tool list. The surface also keeps outrunning the announcements — the `sf-data360` extension, the `data360_*` v2 dispatcher and now this all arrived inside repositories, with no press release.
3. **Agent Script + the new Agentforce Builder are GA — and the language now outruns the tooling that deploys it.** — since the week of **July 13, 2026** the *New Agent* button opens nothing else — and Agent Script is **open source** (Apache 2.0). But the platform now authors agents **two contradictory ways**: Agent Script (explicit, version-controlled) and the **digital blueprint** of Agentforce Operations (generated from a document or diagram, edited in prose). One governance model does not cover both. **Agent Script 3.x** added a second agent *topology* — `GoalBasedAgent`, cron-triggered, no user turn — on 08-19; five days later `sf agent publish authoring-bundle` still could not publish anything using `connected_subagent`, in either topology. Treat "the language supports it" and "you can ship it" as separate dates.
4. **Data 360 is the context engine, and it finally has a first-class Apex surface.** The rename is real (SKUs, release notes, the consultant cert). Winter '27 adds the **`sfsqlquery`** namespace — `SqlStatement`, `SqlRowIterator`, `Row`, `QueryHandle`, `SqlQueueable` — so an agent action can compute a live aggregate instead of reading a scheduled insight. The dataspace trap survives intact: omit it on a DLO query and get **zero rows, silently**.
5. **Security defaults flipped, the boundary runs both ways, and commercials moved to consumption.** At API 67.0, Apex DML/SOQL run in **user mode** and classes default to **`with sharing`**; the **metadata pipeline is an inbound trust boundary too** (the SDR zip-slip); and IL5 showed the Trust Layer decides **which models exist at all** in an environment. On price: **Flex Credits** ($500 / 100k, ~$0.10 per action), **pay-per-resolution** at $2 for the Help Agent, and negotiated **AELAs** at the top end.

**Newest additions (scan of 2026-08-25, 03:45 UTC — 72h window; no scans ran on 08-23 or 08-24; weekly consolidation run):** an **eighteenth consecutive scan with no Salesforce product announcement**, nothing from Data 360 or AI Research — and the run still produced four entries and **three corrections to this radar's own record, one of them about a security patch.** **The lead is a file nobody opened.** `@salesforce/cli` publishes an **`npm-shrinkwrap.json`** — 1,759 packages, `lockfileVersion` 3, one exact version each — and npm honours a published shrinkwrap, so **every caret range this radar has reasoned about since 08-02 is overridden at install time.** What stable actually carries: `sf` `latest` **2.148.3** → SDR **13.0.1**, `@salesforce/agents` **2.0.1**; `latest-rc` 2.149.9 → 13.1.1 / 2.0.4; `nightly` 2.150.5 → 13.1.1 / 2.0.6. Three consequences, each written back into the entry it overturns. **`sf` 2.147.7 never carried the SDR zip-slip fix** — it pins **13.0.0**, so stable got that patch on **2026-08-19** with 2.148.3, seven days later than reported. **The TOCTOU symlink fix (13.1.1) is still not on stable**, against an 08-13 entry that said *"the TOCTOU fix is on `latest` today"*. And **stable has one of the four `--api-name` preview fixes, not four** — the 08-20 correction claiming a fresh install resolves 2.0.5 by range was wrong in both directions. The durable rule: **when a package can pin its own tree, the manifest is a statement of intent and the lockfile is the fact** — and `npm view … dependencies` is the wrong check. Two escapes exist and are worth naming: `oclif.jitPlugins` (10 packages, installed on first use) and `sf plugins install`. **Second entry: the language outran the deployer.** `@salesforce/agents` **2.0.6** (08-24 20:58 UTC, PR #350) guards `ScriptAgentPublisher` against nodes with no `tools` array — before it, `sf agent publish authoring-bundle` threw `TypeError: … reading 'map'` for **any** bundle containing a `connected_subagent` block, because that block compiles to a **`related_agent`** delegation stub that emits no `tools` key. The existing test covered `tools: []`; the crash needs the property *absent*. **And it is not `GoalBasedAgent`-only** — the compiler's `delegate_escalation.agent` fixture uses it under `AgentforceServiceAgent`, so conversational multi-agent scripts hit it too. It is the **fifth** `@salesforce/agents` 2.0.x defect in fifteen days and the first outside preview. Because of the shrinkwrap, **stable cannot get it before 2026-09-02**: the 08-26 promotion takes `latest` to the 2.149.x line, which pins 2.0.4. **Third, day-of churn a daily scan would have missed:** SDR cut **three releases on 08-24**, and only two reached npm — `13.2.1` returns **`version not found`** despite a `chore(release)` commit, a compare link and changelog prose. Its real fix (#1817) rode **13.2.2** instead, which also carried a regression: a new `readDirectory()` on a computed component root threw **`ENOTDIR`** when that root is a file, so `sf project deploy`/`retrieve` crashed on a stray `README.md` or `.DS_Store` inside `lwc/`. That build was npm `latest` for **4h25m** before 13.2.3. The fix underneath is worth keeping: resolving a **decomposed Permission Set** directory now requires the file's suffix to match `type.suffix`/`legacySuffix`, so `permissionsets/myPS/` stops returning a child as the component. No CLI channel was ever exposed — the shrinkwrap again. **Fourth: Salesforce's own tooling now treats its skill catalogue as a context cost.** `sf-pi` **v0.272.0** (08-22, **ADR 0108**) stops wiring Pi to the `sf-skills` clone, loads a stamped sidecar at `~/.pi/agent/sf-skills/effective/skills`, and makes managed-library skills **`manual-only`** by default — `disable-model-invocation: true`, still reachable by `/skill:<name>`, out of the system prompt. The ADR says why: at 164 skills the descriptions *"dominated first-turn context in measured sessions."* Resolution short-circuits in a fixed order, and **community-origin skills stay `agent-invocable` while Salesforce's own go dark**. Intent lives in `sfPi.skillInvocation`; fifteen packs match by name prefix; the catalogue grew 138 → 164 on 08-21 and was hidden on 08-22. **Method note:** every finding came from npm tarballs and shallow clones; the newsroom returned nothing for a fourth straight run, and `www.salesforce.com` was `EGRESS_BLOCKED`.

**Previous additions (scan of 2026-08-22, 03:36 UTC — 24h window, extended to 72h for one repository artifact):** a **seventeenth consecutive scan with no Salesforce product announcement**, nothing from Data 360 or AI Research — and one npm release that carried the entire run. **`forcedotcom/sf-skills` 1.41.0** (2026-08-21 14:48 UTC, *"Release 26 new + 11 updated skills"*) is the largest single release this radar has read: **138 → 164 skills**, none removed. **The lead is a preference that no API can write.** The six-skill CMDB set recorded on 08-14 is now a **four-track ITSM program** — Incident Management, Agentforce for ITSM, CMDB, Microsoft Teams — under one orchestrator, with **18 new `service-itsm-*` skills** and prerequisites deliberately pushed down into the sub-orchestrators so the menu still runs on a bare org. `PATCH /setup/org/preferences/ITSMTeamsEnabled` returns **`401 INSUFFICIENT_ACCESS`**, always, because the preference's UDD definition declares `orgAccess="always"` with **no `editAccess` attribute**; the working path is `POST /connect/setup/discovery/feature/service-cloud-itsm-teams-integration/enable`, which flips it as a side effect. **That generalises the CMDB finding into a rule: a Salesforce Go page toggle is a read model of a feature, and a `401` there means wrong route, not wrong credentials.** Downstream, the Teams track publishes field notes worth more than most documentation — `/connect/ms-teams-app/tenant-config` is a **`403 FUNCTIONALITY_NOT_ENABLED [MsTeamsAppApiFamily]` dead end with no self-service unlock** (seven candidate Go feature names all `400 NOT_FOUND`; none of 112 `PermissionSetLicense`s grant it); portal SSO resolves on **`User.Username`, not `User.Email`**, through one Apex handler `MsTeamsItsmSSOHandler` serving both apps with no JIT; the Azure redirect URI must sit under the **Web** platform, not SPA, or the callback dies with `OAUTH_APPROVAL_ERROR_GENERIC` *after* a successful `LoginHistory` row; and `AuthProvider.ConsumerSecret` is **not `updateable`**, so it goes in by source-format deploy or not at all. The same release introduces the **Unified Employee License (UEL)** as a four-record chain — `User` + Person `Account` + `PersonContact` + `Employee2` — carrying **exactly one** permission set, `EmployeeHubEmployeeUser`, which is managed, uneditable, and does **not** grant `ApiEnabled`. **Second entry: the hosted dispatcher is displacing the CLI.** Skills declaring `mcpTools` went **13 → 24** and those naming **`headless-360` went 4 → 14**; `experience-portal-create` is the first non-ITSM one and the first skill in the catalogue to forbid the CLI in writing — *"`dispatch`/`dispatch_readonly` is the only way this skill talks to the org."* **That qualifies the 08-11 reading of Skills → CLI → MCP**: the precedence orders *instruction* sources, not execution. The catalogue also became self-describing for the first time (`releaseRef` **1.41.0** / 164) — and the 08-17 entry is **corrected**: 1.39.0 was not a fix, because 1.40.0 shipped a stale ref again. **Third, from a 72h reach: Winter '27's metadata roster is still moving.** SDR's generated `METADATA_SUPPORT.md` has grown the v68 section **59 → 71 types** since 08-13, **11 of the 12 arrivals unsupported by source-format DX**, led by an undocumented five-type **`TenantSecurityPlaybookDef` / `…DefStep` / `…DefVer` / `TenantSecurityStepDef` / `TenantSecurityStepDefVer`** family added 2026-08-20 22:09 UTC. Its trap is a dating one: the file is regenerated nightly, so any count needs a timestamp, and its newest commit is a **915-line Prettier reflow that changes nothing**. **Method note:** every finding came from diffing two npm tarballs and reading frontmatter and `references/` — the changelog line said "26 new + 11 updated skills" and nothing else.

**Previous additions (scan of 2026-08-21, 03:36 UTC — 24h window, plus a gap check):** **a quiet window, and the gap check paid for it again.** The window itself yielded **one correction and no new entries**: `@salesforce/react-native-agentforce` **0.6.0** reached npm on **2026-08-20 20:23 UTC** and its GitHub release — titled **262.1.3**, no RC suffix — carries the **Latest** badge, closing the RC-on-`latest` exposure the 08-17 scan opened. It ran **seven days**. Native pins came off RC with it: iOS **AgentforceSDK 18.26.9-rc4 → 18.26.17**, **AgentforceService 6.11.3-rc1 → 6.11.4**, Android **`agentforce-sdk` / `agentforce-sdk-voice` 15.130.3-rc1 → 15.130.4**; **AgentforceVoice stays pinned at 2.8.2**, so the Voice conflict survives the upgrade, and the package still publishes **exactly one dist-tag**. A second timing rule falls out: **`da04dcc` set the version to 0.6.0 on 08-14 and npm got it six days later** — the publish tracks the GitHub *release*, not `main`, so reading the repo tells you what is coming and never what `npm install` resolves. **Then the gap check, on `ai-research-and-benchmarks.md` at 13 days stale, produced three entries and ended an eighteen-scan AI Research drought.** **The lead is an auth fact wearing a benchmark's clothes.** SCUBA merged **PR #7** on 08-18 from a branch authored **2026-05-07** — 103 days on a branch, so the repo looked quiet while the fix already existed. Parallel browser logins now serialise behind an `asyncio.Lock` and a one-second stagger, because `/services/oauth2/singleaccess` mints a **single-use** frontdoor URL and minting several concurrently *for one user* fails. **That generalises past the harness:** session bootstrap is a serialisation point for any fleet of UI-driving agents sharing one identity, and the token it caches — `data/oauth_refresh_token.json`, plaintext, **`full` scope** — belongs nowhere but a throwaway org. It is SCUBA's **third** authentication fix; the first two said auth is where these projects *start* badly, this one says it is also where they **stop scaling**. Second: **`self-improve-fragility`** (Apache-2.0, code 2026-07-16, governance refresh 08-11) measures memory-based self-improvement — **Agent Workflow Memory** and **ReasoningBank** — across WebArena, VisualWebArena and SCUBA. **Its protocol is the finding**: 12 developer orgs, `scuba_{baseline,synapse,awm,rbank}{1,2,3}`, four methods × three runs, a **fresh org each run**, because a single run cannot separate improvement from the three axes in the paper's title — variance, task order, underspecification. **No paper is locatable**, so there is no number to quote and none was invented. Third: **ClaimWriter / ClaimProbe** (2026-08-05, **CC BY-NC 4.0**) is a claim-level faithfulness audit with six published metric definitions — `HALL`, `HALLstr`, `MIS`, `UNC`, `REC`, `RECstr` — matching **report→source strictly and source→report permissively**, and it grades **Enterprise Deep Research** alongside NVIDIA AI-Q and Open Deep Research. **`UNC` is the one to internalise: high `UNC` with low `HALL` is under-citation, not hallucination.** Its `CITATION.cff` reads *"Authors to be added after publication"* — **the paper does not exist yet**, and `results/README.md` states the release omits the corpora, judge traces and aggregate tables, so its numbers are **not reproducible from this code**. **Two licences, one fortnight, one organisation:** Apache-2.0 and CC BY-NC 4.0 — the standing rule holds per artifact, never per org. **Method note:** every finding came from `git clone --depth`, and the org's repository list *sorted by last update* is what surfaced two repos that had never been recorded; the newsroom search returned nothing again — a **sixteenth** consecutive scan with no Salesforce product announcement.

**Previous additions (scan of 2026-08-20, 03:42 UTC — 24h window):** **the drought broke twice over.** After fifteen scans with no Salesforce product announcement, **2026-08-19 produced one** — *"Expanding Headless 360: Enterprise Capabilities"* — and, separately, the richest single language drop this radar has read: **Agent Script cut three majors in one open-source sync**. Four entries and two corrections. **The lead is a second agent topology.** Two internal→OSS syncs landed in `salesforce/agentscript` on 2026-08-19 (358 files, +36,856 / −11,734), taking `@agentscript/agentscript` **2.23.0 → 3.4.0**, the compiler **2.42.1 → 3.8.1** and the language package **2.20.0 → 3.2.3**. They introduce **`config.agent_type: "GoalBasedAgent"`**, internally **AgentIQ**, and it is not syntax — it is a different agent shape. A **`trigger:` block fires a `workflows:` entry on a cron schedule** (`schedule: "*/5 * * * *"`, `target: @workflows.engineering`) with **no user turn**; **`orchestrator` replaces `start_agent`** as the entry point; a workflow dispatches to a `connected_subagent` (`target: "agent://Agent_1"`) or a bare `prompt:`. **The two topologies are mutually exclusive and the linter enforces it both ways** — `bundles`, `workflows`, `trigger`, `actions` and `orchestrator` raise **`gba-only-<block>`** outside a GBA; `subagent` and `start_agent` raise **`gba-forbidden-<block>`** inside one. **The architect's fact is the absence:** a scheduled run has **no inbound turn to authorise against**, so sharing, FLS and record ownership follow an identity the script never states. Same major also **governs `additional_parameter__`**: six fields deprecate to `config.runtime.*` and **`additional_parameter__disable_graph_runtime` is a hard error** with no replacement. Second entry, from the earlier sync: **Agent Script voice gets a v2 schema** — flat `inbound_*`/`outbound_*` keys become nested `inbound:`/`outbound:` blocks, with per-locale `language_settings` (BCP 47), `session_language_switching` (`Monolingual`/`Multilingual`), addressable ASR/TTS `model.id`, and `pronunciations` in **IPA or CMU**. **Mixing v1 and v2 is a compile error**, not a merge, and v2 lands under a separate **`voice2_config`** key. Third: **the announcement, read against yesterday's finding, is a warning rather than a feature.** It names a hosted **Data 360 MCP Server** (~200 APIs), **prebuilt Data 360 Skills at GA "targeted August 2026"**, a **Slackbot MCP Client** across 20+ partner apps, and an **Agentforce Experience Layer** (zero mentions study-base-wide). But **`forcedotcom/d360-mcp-server` has not moved since 2026-07-02** — still Developer Preview, STDIO-only, one user/org per process — the hosted `headless-360` reference page is **still titled Beta**, and the `data360-*` skills you can install today still bottom out in a 3-star personal repo. *"Prebuilt Data 360 Skills, GA this month"* and *"the Data 360 skills that exist"* **are not the same artifact**. The repo's own README also beats the press release for detail: **201 operations in 22 families** behind the three facade tools. Fourth: **`@salesforce/plugin-data-code-extension` 1.4.0/1.4.1** finish porting `deploy` and `run` off the `datacustomcode` Python binary — native TS deploy over Data Cloud REST with a presigned-URL upload — and the consequential half is the auth model: **`--target-org` replaces credential profiles, which the commit says are *intentionally* not ported**. **Two corrections.** `sf` **`latest` is now 2.148.3**, so the `org generate password` floors (`--length` ≥ 20, `--complexity` ≥ 3) **are on stable** — and yesterday's "it missed its own stated date" was **a timezone artifact**: 03:47 UTC on 08-19 is 20:47 PT on the 18th, before the promotion window opened. **Dist-tags move on Pacific business hours; a pre-dawn UTC check reads the previous day.** And **`@salesforce/agents` 2.0.5 is not a fifth `--api-name` defect** — it fixes the library's own NUTs; the count stays at four. **Method note:** the announcement was found by search, every first-party page was `EGRESS_BLOCKED`, and both Agent Script entries came from a clone of a repository that had been quiet since 2026-08-03 — a 17-day silence that ended with a major.

**Previous additions (scan of 2026-08-19, 03:47 UTC — 48h window; no scan ran on 08-18):** a **fifteenth consecutive scan with no Salesforce product announcement** — and the eight-scan Data 360 drought broke, from a repository rather than a press release. **The lead is a provenance failure.** The seven Salesforce-published `data360-*` Agent Skills — Apache-2.0, in `forcedotcom/sf-skills`, now shipping inside the official Claude Code plugin — bottom out in **`Jaganpro/sf-cli-plugin-data360`**: MIT, **3 stars**, 23 commits, **159 commands across 23 topics**, and by its own README *"NOT an official Salesforce product… unsupported, experimental… for internal exploration and demo purposes."* It is **not on npm**, so `sf plugins install` cannot reach it; you `git clone`, `yarn install`, `npx tsc`, `sf plugins link .`, and there is **no version to pin** — the setup doc tells you to track the latest upstream commit. **And 1.39.0 deleted the one line that said so**, a top-level `compatibility:` string present on all seven skills at 1.38.0; `compatibility` now appears zero times repo-wide while `data360-query` alone still issues `sf data360` twelve times. `cliTools` cannot express the gap — it declares only `node >=18.0.0` and `sf >=2.0.0`. Three aliases to recognise (repo `Jaganpro/…`, oclif package `@gthoppae/…`, stale clone path `gthoppae/…`), and the documented repair is a **`curl … | python3 -` from a personal account**. Same entry: `data360-code-extension-generate` gives `sf plugins install @salesforce/plugin-data-codeextension` **four times**, including both "Plugin not found" tables — that package **does not exist**; the real one is `@salesforce/plugin-data-code-extension` (**1.4.0**). Second entry, from the same repository: **the plugin's capability catalogue was a snapshot frozen six releases back.** `discovery.json` declared `publicRelease.releaseRef: "1.32.0"` and 102 public skills at tags 1.36.0, 1.37.0 *and* 1.38.0; **1.39.0** refreshed it to **1.38.0 / 138**. That ref is not cosmetic — it is the `#<ref>` in the pinned install the plugin executes, so for a fortnight the guarded add flow was installing stale skills with no error, and 36 newer skills were not addable at all. **1.40.0** then added **`metadata.domains`**, the fifth frontmatter field, across all 138 skills — and it is a **second taxonomy**: Title Case and multi-valued (`"Data 360"`, 1–3 per skill), while the catalogue keeps a singular lowercase prefix-derived `domain` (`data360`) that is what `sf-context discovery domain` actually reads. `domains` appears **zero** times in `discovery.json`. Third: **`@salesforce/agents` 2.0.3 and 2.0.4**, six minutes apart, make it **four defects in nine days in `sf agent preview --api-name`** — after `bypassUser` (2.0.1) and context variables (2.0.2) came a missing `x-attributed-client: no-builder` header, which *removes markdown from responses*, and **no reasoning traces at all**: `ProductionAgent.getTrace()` returned `undefined` unconditionally. The pattern is the finding — `--api-name` runs a **different client class** (`ProductionAgent`) from `--authoring-bundle` (`ScriptAgent`), and **the path that matches production is the less complete one**. Reachability is the safe direction: stable `sf` 2.147.7 pins `plugin-agent` 2.0.0, which *ranges* `^2.0.0`, so a fresh install resolves 2.0.4 — `^2.0.0` **permits** the fix, only `plugin-agent` 2.0.3's `^2.0.4` **compels** it, and that is on `nightly` alone. **Two standing facts corrected.** `sf-skills` is **no longer a Friday-only release train** — three releases in five days, 1.38.0 (Fri), 1.39.0 (Mon), 1.40.0 (Tue). And **`sf` 2.148.3 was not promoted to `latest` on its own stated date**: at 03:47 UTC on 2026-08-19 `latest` is still 2.147.7, so the `org generate password` floors have not reached stable. **Method note:** all three entries came out of one shallow clone of a repository already on the weekly list — the finding was in reading the *frontmatter and reference files*, not the changelog.

**Previous additions (scan of 2026-08-17, 03:36 UTC — 24h window):** a **fourteenth consecutive scan with no Salesforce product announcement**, a **seventh** with no Data 360 movement and nothing from AI Research — and an empty window that still paid, because both entries came from re-reading sources this radar had already opened and taken only part of. The lead is a **breaking change with a date two days out**: from `sf` **2.148.3**, scheduled for `latest` on **2026-08-19**, `sf org generate password` **errors** on `--length` below 20 or `--complexity` below 3 instead of silently raising them (`@salesforce/plugin-user` **5.0.0**, npm 2026-08-10 22:34 UTC, both changes marked `feat!`). **The interesting part is what the old behaviour concealed.** A setup script asking for a 12-character password never got one — so the value in source and the value applied were free to disagree indefinitely, and a silently-corrected input is an unverifiable control. The break makes an old lie audible without changing anything an org receives. **And only half of it was ever deprecated:** the 2026-04-01 notice warned about `--complexity` alone — *"Starting in Summer '26, the command will fail"* — and never mentioned `--length`, so anyone who acted on it fixed the announced half. Reachability runs the **safe** direction of the 08-14 pinning rule: `sf` **pins** `plugin-user` exactly, so no lockfile or fresh install pulls 5.0.0 early and none dodges it once 2.148.3 is `latest`. There is no partial rollout, only a date. Two smaller changes ride the same release — **`--skip-assignment-rules`** on `sf data create record` / `data update record`, and **`api request rest` / `api request graphql` reaching GA**. Second entry: **`@salesforce/react-native-agentforce` 0.5.0 is a release candidate sitting on npm `latest`.** The matching GitHub release is titled **262.1.3-RC4** and **marked Pre-release**, while GitHub's "Latest" badge still sits on 0.4.0. The package publishes **exactly one dist-tag**, so there is no `rc` or `next` to opt into and no stable tag to fall back to — a default install ships an RC into a mobile app. It brings direct launch into **Voice mode**, per-agent splash screens and exposed voice caption defaults, with **iOS Agentforce Voice pinned to 2.8.2**. **Method finding — the miss was in the re-reading, not in the window.** The 08-13 scan read this exact CLI release-notes file and took two things from it (the Wednesday cadence, the Claude Code marketplace correction) without ever enumerating what 2.148.3 changes; the 08-14 scan read npm for six packages and not for the mobile bridge. Neither was a coverage gap — both sources were on the weekly list and both were opened. **Reading a source for the question you arrived with is not the same as reading it.**

**Previous additions (scan of 2026-08-16, 03:39 UTC — weekly consolidation run, 48h window):** a **thirteenth consecutive scan with no Salesforce product announcement**, a **sixth** with no Data 360 movement and nothing from AI Research — and the richest single release this radar has read out of a repository, plus a correction to one of its own standing notes. The lead is a product enablement path that shipped with no announcement at all: **`forcedotcom/sf-skills` 1.38.0** (2026-08-14, 121 → **138** skills) adds six `service-itsm-*` skills, five of them a coordinated **CMDB** (Configuration Management Database) setup set for Service Cloud ITSM. What makes it an entry rather than a changelog line is that it **publishes the gate**: every CMDB Connect API checks `orgHasCMDBEnabled = orgHasCMDBPermission (org perm ITSrvcsCnfgMgmnt) && OrgPreferences.CMDBEnabled`, and failing it returns **`403 FUNCTIONALITY_NOT_ENABLED`** — **the same code at every one of six ordered layers**, which is exactly the shape that costs a project days. **Layer 0 is the architect's fact: `ITSrvcsCnfgMgmnt` comes from the edition or licence and no API can grant it**, so "we'll enable it later" does not exist; the cheapest probe is querying for the PSL `ItSrvcCnfgItmReadPsl`. Four permission sets and their backing PSLs are named exactly, and **only Type Manager clears `403` on bundle operations**. Second, and the reason the state-of-play point above was rewritten: **`headless-360` reached skill frontmatter and its consumers describe a dispatcher**. `mcpTools` went **7 → 13 skills and 3 → 6 servers** in one release, with `headless-360` going from **zero mentions anywhere in 1.37.0** to five skills; those skills document **four meta-tools**, `dispatch` taking **raw HTTP** (`queryParams`, camelCase — `query_params` is rejected), the org derived from the **session's OAuth JWT**, and a first-ever named org gate **`api.agentic.access.enableHeadless360McpServer`**. One trap is worth more than the shape: **a `discover` miss does not prove a route is absent** — dispatch the exact path and believe only a real `404`. Third, a **correction to this radar's own standing note**, and the compulsory `pricing-and-certification.md` gap check paid for itself: **`sf-skills` is Apache-2.0 on GitHub and CC BY-NC 4.0 on npm.** The package `@salesforce/afv-skills` declares `"license": "CC-BY-NC-4.0"` on **every published version** — all 25 from 1.14.0 (06-03) through 1.38.0, including the twelve after the 2026-06-29 Apache relicensing — while the same tarball ships a **byte-identical** Apache-2.0 `LICENSE.txt` (md5 `609f9a6b02ce4af3f8b94db949dec35c`). The 08-01 rule survives and sharpens: **the licence attaches to the channel, not the repository** — and the 08-01 study action, which greps `LICENSE`/`LICENSE.txt`, is precisely the check that cannot find this. Fourth: **`@salesforce/agents` 2.0.2** fixes `sf agent preview --api-name` dropping **context variables** — the second silent defect in five days in the same path, both because **`--api-name` initialises a preview session differently from `--authoring-bundle`**. **One negative closed:** no `data360-*` MCP server appears in any `mcpTools` block across all 138 skills, answering the 08-14 watch item.

**Previous additions (scan of 2026-08-14, 03:41 UTC — paragraph added at the 08-16 weekly pass):** three tooling entries and a method finding, all from repositories. **SDR 13.1.1** patched a **TOCTOU symlink escape** in `staticResourceMetadataTransformer.ts` — the same file and class as the zip-slip 13 days earlier — where the malicious input is a symlink **in your own working tree**, not the org. **`mcpTools`** arrived in `sf-skills` 1.37.0 as the fourth field in the skill dependency contract, naming four MCP servers, three never announced. **`sf project retrieve start --root-type-with-dependencies`** shipped with a closed enum of exactly `Bot` and `AiAgentDefinitionVersion`. The durable lesson is the **pinning asymmetry**, which corrected the 08-13 framing: **`sf` pins each plugin to an exact version and each plugin ranges its libraries**, so a library security patch reaches stable on a fresh install while a plugin feature cannot until a new `sf` ships. Ask which layer a change lives in before predicting whether it can reach you.

**Previous additions (scan of 2026-08-13, 03:37 UTC):** an **eleventh consecutive scan with no Salesforce product announcement**, no Data 360 news and nothing from AI Research — and yet the most consequential tooling window in a fortnight, because **the thing this radar had been watching for eleven days finally happened, and it turned out never to have been a wait at all.** `@salesforce/cli` **`latest` moved 2.146.3 → 2.147.7** on 2026-08-12; every dist-tag advanced one slot (`latest-rc` → **2.148.3**, `nightly` → **2.149.0**). Three things arrive on stable together: a **hard Node floor** (`engines.node` `>=18.6.0` → **`>=22.0.0`**, installer runtime Node 24), the **SDR zip-slip fix** (plugin-deploy-retrieve **4.0.1** ranges SDR **`^13.0.0`**), and **employee-agent preview** (plugin-agent **2.0.0** ranges `@salesforce/agents` **`^2.0.0`**, resolving 2.0.1). **The correction is to this radar's own framing.** Five scans called the unmoving `latest` tag a *stall*; the CLI release notes state the cadence outright — **stable ships on Wednesdays**, and 2.147.7 soaked as `latest-rc` for exactly one week after its 08-05 npm publish. It was a schedule, published in the file, read as a decision. A second dating trap from the same file is worth more than the promotion: **the release notes date a version by its planned *stable* date, not its npm publish date** — `## 2.148.3 (August 19, 2026)` describes a build published **2026-08-12 03:13 UTC**. And **range resolution, not the pin, is what delivers the fixes**: a stale lockfile or an existing `node_modules` holds you at the broken build while a colleague's fresh install works. Second item, and it **closes the API-version question two scans declared unobtainable from this environment**: `@salesforce/source-deploy-retrieve` **13.1.0** (08-12 19:49 UTC, PR #1819) regenerated its coverage report with a **`## Next Release (v68)`** section — **Winter '27 is API 68.0**, confirmed, from a generated markdown file in a repo this radar reads weekly rather than from an org endpoint. That release also adds **`AiAgentDefinition`** (dir `aiAgents`, suffix `aiAgentDefinition`) and **`AiAgentDefinitionVersion`** (dir `aiAgentDefinitionVersions`, **bundle adapter, no suffix**) — and one line of `types.ts` is the tell: `rootTypesWithDependencies` now accepts **`Bot`, `AiAgentDefinitionVersion`**, registering the new type as a **peer of `Bot`**, a root of an agent's whole dependency graph. That gives Agentforce three metadata layers — **authoring** (`AiAuthoringBundle`, the `.agent` file), **runtime** (the new v68 pair, previously generated and opaque), **reasoning** (`GenAiPlanner*` / `GenAiPlugin` / `GenAiFunction`) — with `Bot`/`BotVersion` as the legacy runtime. Of **59 new v68 types, 21 are DX-supported and 37 are not**, including `AiAgentDefinitionPlanner`, `BotEmailDefinition`, all four telephony-provider types and three security types (`SecurityCustomBaseline`, `ScopedAccess`, `SensitiveDataRuleElmntGrp`). Third, a **correction rather than an entry**: the `salesforce-development` Claude Code plugin recorded yesterday now installs from the **official Claude Code marketplace** — `/plugin install salesforce-development@claude-plugins-official` — per the CLI 2.148.3 release notes, which is exactly the distribution-channel shift the 08-12 note named as *next to watch*.

**Previous additions (scan of 2026-08-12, 03:37 UTC):** a **tenth consecutive scan with no Salesforce product announcement**, no Data 360 news and nothing from AI Research — and for the **third day running both items came out of repositories this radar reads weekly**. The lead is the bigger miss this radar has admitted in a fortnight: **Salesforce ships a first-party Claude Code plugin, and two prior entries about the repository that carries it never named it.** `forcedotcom/sf-skills` **1.36.0** (2026-08-11 07:37 UTC) publishes **`salesforce-development` 1.10.0**, installed from a **plugin marketplace in the same repo** (`/plugin marketplace add forcedotcom/sf-skills` → `/plugin install salesforce-development@salesforce`). It has been shipping since **1.34.0** at 1.9.0. The distinction that matters is **catalogue versus runtime**: `npx skills add` gets you 41 SKILL.md files; the plugin gets you **five agents** (`salesforce-dev`, a read-only `architecture-review`, and the ADLC set `adlc-orchestrator` → `adlc-author` / `adlc-engineer` / `adlc-qa`), **three MCP servers** — one of which, `salesforce-lsp`, lazily spawns the **Apex and SOQL language servers and re-exposes them as MCP tools** — ten slash commands, and **hooks on eight events**, including a production deploy gate and an **auto-deploy hook that fires on any `Edit`/`Write` under `force-app/**`**. The transferable idea is the precedence: capabilities resolve **Skills → CLI → MCP**, so Salesforce ranks deterministic instructions above its own CLI and its own CLI above **its own hosted MCP servers**. 1.10.0 itself adds `ui_mode` (`full`/`compact`/`plain`/`off`), a status line, a **Claude Code ≥ 2.1.222 floor**, a security fix (capability discovery no longer discloses descriptions of skills you have not installed — only ones **verified installed and unmodified**), and a journey fix worth the name: **Test now completes only after a real, successful Apex test run.** Second item, and it **corrects the 08-09 entry**: `sf-pi` **v0.266.0** (08-11 21:59 UTC, 28 files, **−4,857**) accepts **ADR 0106**, making the **v2 registry and dispatcher** the only Data 360 path with live proof — and reading it surfaced **ADR 0027 (2026-06-01)**, never recorded here, which says the eleven `data360_*` tools are **not eleven endpoints** but **family tools over one generated action registry**, called with an action string (`stream.create_ingest_api`, `sql.verify_rows`) and a compact envelope. The flat-facade shape this radar wrote up on 08-09 was the **legacy** `d360` surface. Action names live in a generated-but-committed `registry/v2/actions.json`, every operation resolves to **exactly one** owner, and **headless destructive execution needs four things at once**: a non-production target, `--mutate`, an 8–32-char run ID, and both `SF_PI_D360_V2_SWEEP_MUTATION_TARGET_ORG` and `D360_V2_SWEEP_ALLOW_DESTRUCTIVE`. Third, a **correction rather than an entry**: yesterday's conclusion that the employee-agent preview fix "cannot arrive on the channel most people install from" is now half-overtaken — **`@salesforce/plugin-agent` 2.0.1** (08-11 15:32 UTC) pins `@salesforce/agents` **`^2.0.1`** and **`sf` 2.148.3** (08-12 03:13 UTC) pins that plugin, so the fix rides a released CLI rather than resolving by luck. **`latest` is unchanged for a sixth day** at 2.146.3, and 2.148.3 is `nightly`.

**Previous additions (scan of 2026-08-11, 03:38 UTC):** another quiet product window — **no Salesforce Agentforce announcement for the ninth consecutive scan**, no Data 360 movement, nothing from AI Research — and both items that cleared the bar came from the same place as yesterday's: **repositories this radar reads weekly, not announcements.** The lead is a **breaking change that corrects yesterday's lead.** `sf-pi` **v0.263.0** (2026-08-10, 139 files, +4,594 / −1,914) routes every Salesforce connection through one new module, **`lib/common/sf-conn`**, and **deletes the JSforce `50.0` fallback**. Yesterday's entry concluded the v0.262.1 fix changed *"honesty, not behaviour — the fallback is still 50.0."* **That held for one day.** The new rule is also the interesting one, because it **inverts the usual precedence**: the org's **highest advertised `/services/data` version wins even over a configured `org-api-version`** — discovery is authoritative, config is the fallback — and when neither is available the operation is **rejected before the request is sent** rather than degrading to a 2021 default. Practical consequences: `org-api-version` **no longer pins anything**, and **Data 360 raw REST callers must pass versionless paths** (a caller-owned `/services/data/vNN.N/…` is rejected, not rewritten). One 403 detail worth keeping: only a *definite expired session* triggers the shared auth refresh, so a **403 is never replayed** — it means permissions, not auth. Second item: **`@salesforce/agents` 2.0.1** reached npm at **21:36 UTC**, fixing `sf agent preview start --api-name` for **employee agents**. The flag is the lesson — **`bypassUser` encodes which identity an agent runs under**: customer-facing agents run with **no Salesforce user**, employee agents (`AgentforceEmployeeAgent`) run **as the authenticated one**, and sharing, FLS, `UserInfo` and record ownership all follow from it. The `--api-name` path hard-coded `bypassUser: true` for both, so every employee-agent preview died on **`400 Invalid user ID`** while the same agent previewed fine from `--authoring-bundle`. **And the reachability is the zip-slip pattern one package over:** stable `sf` **2.146.3** ships `plugin-agent` 1.45.0 pinning `@salesforce/agents` **`^1.11.1`**, a range no 2.x build can satisfy — the fix exists, is published, and cannot arrive on the channel most teams install from. **Two negatives closed**, both the attribution checks flagged yesterday: **arXiv 2602.17017 is Microsoft** (Dynamics 365 Sales) and **arXiv 2606.07462 is Xi'an Jiaotong / Xidian** — neither is Salesforce AI Research, and neither belongs in this radar. **One new gap opened:** the `sf-pi` CHANGELOG carries **three undated breaking changes this radar has never recorded**, led by Agent Script publication becoming **eval-gated** (`publish activate=true` removed, `acknowledge_untested_activation=true` as the escape hatch).

**Previous additions (scan of 2026-08-10, 03:36 UTC):** a quiet window — **no Salesforce product announcement for the eighth consecutive scan**, no Data 360 movement, nothing from AI Research — and two items still cleared the bar, one of them a break with a deadline. The lead came from a **gap check on the open question raised yesterday** (*what else is in Winter '27?*), and it found a second Release Update that stops integrations: **support ends for API traffic that uses an incorrect instanced URL**. An instanced URL is a hard-coded instance host — `na45.salesforce.com` — where the My Domain host belongs. **The interesting part is how it differs from the OAuth retirement.** That one is a *pattern* you can grep for. This one is a **value that was correct when someone typed it** and became incorrect later, silently, because Salesforce migrated the org to a different instance; nothing in the codebase records that it went stale. And there is **no date to plan against** — enforcement is *rolling*, "shortly after your org gets this release", so a clean upgrade weekend is not evidence you are clear. The only way to know is to cause the failure yourself: Setup → **My Domain** → **Redirections** → *Block API traffic that uses an incorrect instanced URL*, **testable since 2026-06-19**. The authoritative base URL is the **`instance_url` in the OAuth token response**, never a literal. A third Winter '27 Release Update was also located — a **base-component accessibility batch** — named with no per-component detail, so it is one bullet rather than an entry. Second item, from the window itself: **`sf-pi` v0.262.1 stopped reporting JSforce's `50.0` default as an org fact.** API version status is now **provenance-aware** — `configured`, resolved from `sourceApiVersion`, or **"unverified SDK fallback"** — and it separates *Project Source API* from *Connection API*, which are allowed to disagree. `50.0` is **Spring '21**, and that gap is load-bearing: **user mode and `with sharing` arrive at 67.0**, so a tool quietly operating at 50.0 means reasoning about behaviour you do not have. The fix changes **honesty, not behaviour** — the fallback is still 50.0, it is merely no longer disguised as an observation. New command `/sf-org refresh`. The two other sf-pi releases in the window (`sf-welcome`, `sf-slack`) are display-only and were dropped. **Method note:** egress was narrower than usual again — every `*.salesforce.com` host, `api.github.com` and even `login.salesforce.com/services/data/` (which would have settled the API 68.0 question unauthenticated) returned **EGRESS_BLOCKED**. The instanced-URL entry is therefore recorded **entirely from search-result snippets** and says so.

**Previous additions (scan of 2026-08-09, 03:55 UTC — weekly consolidation run):** the 24-hour product window was empty, and the run is still one of the more consequential this radar has had, because **the Winter '27 release notes went live** — the event flagged as "the most likely source of the next substantial entry" on 07-28, and open ever since. The lead is a **Release Update, not a feature: the OAuth 2.0 username-password flow retires for connected apps** (`release-notes.rn_security_unpw_flow_retirement.htm`). Any integration posting `grant_type=password` **stops receiving a token on its org's Winter '27 upgrade date** — and there is no single such date: production weekends are **2026-08-29 / 10-03 / 10-10**, per instance, read from Trust Status → Maintenance. **Neither replacement is a drop-in.** The web-server flow needs a callback URL and consent; client credentials needs a **run-as user** and returns **no refresh token** — and both change the identity the integration runs as, so sharing and FLS can change what it sees. A functional break wearing an auth costume. The hard clock is **Aug 27, 18:00 PT**: miss that sandbox refresh and the migration reaches production unrehearsed. Same release, second enforcement: **Email Change Verification loses its Support-disable escape hatch**, so bulk user-email updates now need per-user verification. Second item, and the one that ends a seven-scan drought: **Data 360 has a real Apex surface at last.** The Winter '27 notes name the **`sfsqlquery`** namespace — `SqlStatement`, `SqlRowIterator`, `Row`, `QueryHandle`, `SqlQueueable`, three query shapes — which is **the named class list behind the "SQL from Apex" sentence this study base had cited five times with no class in it**. `SqlQueueable` inherits the **stack-depth-50** queueable ceiling, and the dataspace trap is unchanged. Third: **`sf-pi` v0.260.2 added guardrail rule `pi-credential-output`**, gating `pi auth print-api-key` / `print-bearer-token` / `check --credentials` — **the first guardrail here that protects the developer rather than the org**, matched on the *parsed* command so `sudo`, `bash -c` and `npx` wrappers do not evade it. Its trap is that **"Allow for session" is inherited by `/resume` and `/fork`**. Two gap checks paid. The first closed the 08-08 open question: **`sf-data360`** is a **stable, default-enabled** sf-pi extension exposing **eleven `data360_*` tools** with **no MCP runtime** — not the Data 360 MCP Server, and invisible to anyone grepping `sf-skills`. The sourcing lesson is the durable one: **seven scans of "Data 360 is quiet" were drawn from press releases, while both of today's Data 360 items sat in a query guide and a repo this radar already reads weekly.** The second gap check found **Agentforce Operations** — **GA since 2026-04-29, 102 days, zero mentions study-base-wide**, including in the buy-vs-build framework whose whole job is to list the prebuilt families. It is the fifth family, it works on *processes* rather than people, and its **digital blueprint** authoring model is the deliberate opposite of Agent Script. Recorded with the caveat that **no first-party source was reachable** for any of it.

**Previous additions (scan of 2026-08-08, 03:20 UTC):** **the drought broke.** After seven consecutive scans with no Salesforce product announcement, **2026-08-05 produced two press releases and the most consequential governance item this radar has carried**: **Agentforce 360 is authorized at US Department of Defense Impact Level 5 (IL5)** — the highest level for **Controlled Unclassified Information** and **National Security System** data — delivered through **Missionforce National Security** on **AWS GovCloud**, with the **U.S. Army Human Resources Command** the first Department of War organization to deploy it (9.2M people served, 1,500+ cases/day summarized, **55M+ agent conversations/month projected**). **The detail that matters to an architect is not the win, it is the price of the accreditation:** Salesforce had to attest to the Pentagon that **Anthropic-supplied generative AI models were disabled** in that environment. So the Einstein Trust Layer does not only decide *which safeguards run* — it decides **which models exist at all**, and that roster belongs to the **environment, not the org**. Per-agent model pinning and BYOM designs are portable across orgs and **not necessarily across commercial → GovCloud → IL5**. Second item, and the one with a deadline: **the zip-slip status changed**. `@salesforce/cli` **`latest` moved 2.145.6 → 2.146.3** — the *unpatched* release candidate was promoted to stable, so upgrading today still resolves SDR **12.37.2** — while **`latest-rc` became 2.147.7**, the Node 22 line that pins plugin-deploy-retrieve 4.0.1 → SDR 13.0.1. **The fix is on a customer-facing channel for the first time**, and the 08-02 reading that the queued RC "cannot carry the fix" is now corrected: that candidate shipped unpatched, and the RC slot moved. Third: **`forcedotcom/sf-skills` released twice in one Friday** — **1.34.0** added a bidirectional **`metadata.relatedSkills`** field across **79 skills**, turning a flat catalogue into a **graph an agent can traverse**, and hiding a real breaking change (`automation-flow-generate` now requires **API 60.0**, up from 51.0); **1.35.0** added **9 new skills** including two DevOps pipeline skills and `platform-mcp-tool-widget-coordinate`. Fourth: **`sf-pi` v0.260.x closed the Voice eval loophole** — generated Voice suites now declare strict response-integrity automatically, and **exact repeated-surface detection fires even when `llmEvents` evidence is absent**, catching the looping agent the 08-04 gate could only mark `unavailable`. Data 360 was **empty for the sixth consecutive scan**, which triggered the gap check the 08-05 note called for: **"Data Cloud One" had zero mentions in this entire study base** despite being the recommended pattern for any multi-org customer — home org, companion orgs, data spaces as the sharing unit, and the trap that **data residency follows the home org's region**. Now written up with a diagram in [data-360.md](data-360.md).

> **Branch reconciliation (2026-08-08).** The **2026-08-03, 08-06 and 08-07 scans ran but never reached `main`** — each landed on its own `claude/*` branch and none were merged, so every later run read a stale radar and re-derived work already done. All three are now merged. Three items were written up twice or three times and have been **consolidated into one entry each**: the **IL5 authorization** (08-06 + 08-07 + 08-08 → one entry in `trust-security-and-governance.md`), **SCUBA** (08-03 + 08-06 + 08-07 → one entry dated **2025-09**, the paper date), and the **zip-slip dist-tag correction** (four corrections, kept as a dated stack because each records a different state of the release train). Anchors in the affected scan notes were repointed. **If a scan's output is not on `main`, the next scan cannot see it** — that is the failure this pass fixes.

**Previous additions (scan of 2026-08-07, 03:30 UTC — recovered 2026-08-08):** the first scan to record the **IL5 authorization**, and it closed two open questions. **SCUBA** was written up at last ([arXiv 2509.26506](https://arxiv.org/abs/2509.26506), Apache-2.0, 300 tasks from real user interviews, **under 5% open-source / 39% closed-source** zero-shot) — the empirical case for giving agents APIs rather than a cursor. The Data 360 gap check settled the third Summer '26 mystery: the no-code **"semi-joins" builder is a CRM Analytics lens feature in Beta**, doc page `analytics.bi_explorer_data_join.htm` — **not Data 360, not GA**. On tooling, `sf-pi` **v0.258.0–v0.259.2** deleted its bundled gateway model catalogue (**ADR 0077**, 1,071 deletions): model IDs now come **only** from authenticated discovery, so a fresh uncached provider exposes **zero models** until discovery succeeds — an empty catalogue, not a fallback list.

**Previous additions (scan of 2026-08-06, 03:08 UTC — recovered 2026-08-08):** caught the **dist-tag roll on 2026-08-05** at the moment it happened — package `modified` 18:41:04 UTC with no publish after 2.147.7, i.e. **a tag move, not a release**. `latest` went **2.145.6 → 2.146.3** and stayed unpatched; `latest-rc` went to the patched **2.147.7**. **The trap it names is the durable one:** anyone running `npm install -g @salesforce/cli` gets a *newer* CLI and is *still* on the unpatched SDR line, so **a version bump is actively misleading evidence**. It also wrote up the **U.S. Army HRC deployment** as its own entry in `agentforce-platform.md` — 9.2M people, 55M+ projected conversations/month, and the pattern worth stealing: the agent **retrieves and summarizes, the human decides**, with benefits determinations never leaving the specialist.

**Previous additions (scan of 2026-08-03, 03:06 UTC — recovered 2026-08-08):** no product announcement, and the value is in one correction that matters. `@salesforce/cli` **2.147.0** (npm **2026-07-31 14:16 UTC**) — **not 2.147.3** — was the first CLI to require Node ≥ 22 and already carried the SDR patch; 2.147.3 was merely the version sitting on `nightly` when the 08-02 scan read the tag. **A dist-tag tells you where a pointer is today, not when a version first existed.** It also found that the CLI release notes had **no commit since 2026-07-29** while a whole major shipped on `nightly` — read npm, not the notes — and corrected the GIFT-Eval entry: FastCastrage's `org` field was **wrong on the public leaderboard** until 08-03, and **merging is gated on the Salesforce CLA**.

**Previous additions (scan of 2026-08-02, 03:10 UTC — weekly consolidation run):** the window held **no Salesforce product announcement for the sixth consecutive scan**, but it produced the most actionable security item this radar has carried and closed its largest structural gap. The lead: **`@salesforce/source-deploy-retrieve` 13.0.1 fixed a zip-slip** — a path-traversal in `staticResourceMetadataTransformer.ts`, which unzips static resources of `contentType` `application/zip`/`application/jar` during metadata→source conversion, i.e. on `sf project retrieve`. **The interesting part is not the bug, it is the reachability.** There is **no 12.x backport** (newest 12.x is 12.37.2, July 13), `@salesforce/plugin-deploy-retrieve` 3.x pins `^12.36.7` — a range that can never resolve to a patched build — and only plugin 4.x pins `^13.0.0`, which ships in **`@salesforce/cli` 2.147.3**, the first CLI to require **Node ≥ 22** and available **only on the `nightly` dist-tag**. Stable `latest` is still **2.145.6**. So: *"I upgraded the CLI"* and *"I have the fix"* are different sentences, and **npm dist-tags are not ordered by version** — a lesson worth more than the CVE. Second item: **Agentforce Mobile SDK "262.1.2"** is a patch-shaped name over git tag **`18.26.8`**, a Swift 6 strict-concurrency **major**, plus five new Gen UI components (`Table`, `Schedule`, `DataGroup`, `QueryOption`, `VerticalCard`) — read the tag, not the release title. Third, and the reason this run matters beyond a day: **the radar now has a sixth topic file, [`ai-research-and-benchmarks.md`](ai-research-and-benchmarks.md)**, resolving the open question raised on 08-01. Seven scans of research findings that lived only in ephemeral dated notes — **GIFT-Eval's submission protocol, AnchorBench and its licence trap, MFCL Audio, CRMArena-Pro, LoCoBench-Agent, Enterprise Deep Research** — are now durable and in contract shape, with **eleven new glossary terms** and eight flashcards behind them. Housekeeping in the same pass: **newest-first ordering repaired** in `pricing-and-certification.md` (a 07-24 entry sat above three 07-26 entries) and `developer-tooling-and-apis.md` (Agent Fabric and the 06-24 ADLC entry sat above nine 07-26 entries). Data 360 was **empty for the fourth consecutive scan**; its only movement was the reference app `next-gen-wealth` **renaming itself to Agentic Advisor**, which matters solely because it keeps the one public worked example of Data-360-grounded fail-safe prompt templates findable.

**Previous additions (scan of 2026-08-01, 12:18 UTC second pass):** the 24-hour window was **dry** — nothing shipped in `agentforce-adlc`, `agentscript`, `sf-pi` or `sf-skills` since the 03:19 pass — so this run spent itself on **routing**, which is where the value turned out to be. Three findings. First, a routing audit found **substance stranded in ephemeral dated notes**: the **Databricks ↔ Data 360 Unity Catalog file sharing** distinction — *inward* file sharing versus *outward* zero-copy, the pair people reverse in design reviews — had sat only in a 07-27 scan note, and the **Data 360 MCP Server was written up twice** in two topic files with the copies disagreeing. Both now have one canonical home per the contract's tie-breaker, and `agentforce-platform.md`'s broken newest-first ordering is repaired. Second, the 03:19 pass's own output was **rewritten into contract shape** — ~4,000 words of `sf-skills` 1.33.0, dual-licence and `agentforce-observe` substance moved out of the dated notes and into `developer-tooling-and-apis.md`, `pricing-and-certification.md` and `data-360.md`, where it can be learned from. Third, a **Data 360 gap check turned up three Summer '26 features absent from the entire study base** — *Currency Reporting*, *Result Reuse for Data 360 Live*, and a no-code *semi-join / anti-join* builder. They are named in the release notes but no detail is obtainable (`help.salesforce.com` is 403), and the semi-join item may belong to **CRM Analytics rather than Data 360** — its doc page ID is `analytics.bi_explorer_data_join.htm`. **Logged as an open question rather than written up:** a padded entry is worse than a recorded gap. One live increment: `gift-eval` gained **three more submissions (#184–#186, two from LG AI Research), all still open** — making the point that *submitted to* and *on* a leaderboard are different states. **The largest remaining structural gap: the Salesforce AI Research area has seven dated notes and no topic file**, so none of its findings are durable.

**Previous additions (scan of 2026-08-01, 03:19 UTC pass):** no Salesforce product announcement, press release or release-note change landed in the window — the fifth consecutive scan without one — but the repositories were busy. The headline is **`forcedotcom/sf-skills` 1.33.0** (July 31, 17:57 UTC): **10 new + 16 updated skills**, 2,393 files, led by **`service-helpagent-coordinate`**, the first skill dedicated to standing up a **Help Agent** — the product that went **GA in July '26** with pay-per-resolution pricing — via a guided four-checkpoint flow. Also new: three `service-digital-engagement-*` channel skills, three Experience Cloud front-end skills, and **`platform-sandbox-configure` at version 1.0**, which introduces an **`accessCheck`** metadata field declaring the org permission a skill needs (`ManageSandboxes`) — the sibling of the `cliTools` field added July 30. `agentforce-test` **0.8 now covers security testing explicitly** (OWASP LLM Top 10, red-teaming, prompt-injection, a security grade), and `agentforce-generate` **0.11 names `sf agent mcp`** for MCP server registration. This library releases **weekly, on Fridays**. **The most consequential finding is a licensing correction to the July 31 note:** the three `agentforce-*` skills are published in **two** public repositories from the same internal source — `SalesforceAIResearch/agentforce-adlc` under **CC BY-NC 4.0** (non-commercial, blocks client work) and `forcedotcom/sf-skills` under **Apache-2.0** since June 29, 2026 — at **identical versions with byte-identical frontmatter**. Same content, different licence: **the restriction attaches to the copy you took, not to the skill**, so prefer `forcedotcom/sf-skills` for anything commercial. Data 360 was **empty for the third consecutive scan**; the only relevant movement was `agentforce-observe` 0.8, the STDM/Data 360 session-trace skill, shipping inside that Agentforce release — a reminder that Data 360 practitioner surfaces increasingly ship under Agentforce labels. On research, **five external teams pushed results onto the GIFT-Eval leaderboard in a four-minute window on July 31** (PRs #179–#183, now 117 result sets), including one from **Google Cloud AI Research** — a competitor consenting to be ranked on Salesforce infrastructure. But **only one of the five declares `replication_code_available: "Yes"`**, and the leakage field is self-declared: treat a leaderboard position as a claim, not a result.

**Previous additions (scan of 2026-07-30):** the window is non-empty, and the lead item is the first thing in weeks that can **break a green pipeline tomorrow morning**. Between **20:21 and 22:22 UTC on July 29**, the DX Node library stack cut majors on one breaking change: **`@salesforce/core` 9.0.0, `@salesforce/source-deploy-retrieve` 13.0.0, `@salesforce/agents` 2.0.0** all now require **Node ≥ 22** and **drop Node 18 and 20** (both past EOL). That matters because **`@salesforce/agents` implements the `sf agent` command family** (`generate`, `test`, `preview`) and **SDR is the engine behind `sf project deploy`** — so it sits under agent testing *and* under every metadata deploy, **Data 360 metadata included**. The break is quiet, not loud: npm installs on Node 20 with only an `EBADENGINE` warning, then fails later in ways that look like metadata problems. **Check CI images, not laptops** — installer/tarball `sf` bundles its own Node (v24 since February 2026) and is insulated; `npm install -g @salesforce/cli` and `actions/setup-node` pinned to 18/20 are not. Also shipped: **`sf-pi` v0.250.0/v0.251.0** added **native Agent Script quality analysis** — an **18-rule catalogue where High findings block publication** — and the design detail worth stealing, **release evidence that expires on identity rather than on time** ("valid while the exact org, `BotVersion`, baseline identity and suite digest are unchanged"); and **`@salesforce/react-native-agentforce` 0.3.0** reached npm (voice for Service Agents, configurable silence timeout, `UIDelegate` event bridge, native SDKs at the 262.1 line). Two **open, unmerged** PRs are worth knowing but not relying on: an **Agent Script "doctor"** in `agentforce-adlc` ([#46](https://github.com/SalesforceAIResearch/agentforce-adlc/pull/46)) whose **six named anti-patterns** — misleading pipe-text indentation, fake step sequencing, ungated actions, turn-ending setters, stale state, competing side-effect owners — are a review checklist you can apply today with no tooling; and a **GIFT-Eval leaderboard submission** ([#182](https://github.com/SalesforceAIResearch/gift-eval/pull/182)) whose real lesson is the **submission protocol**: 98 fixed configurations, a declared **test-data-leakage flag printed beside every result**, and a required replication link. On Data 360: **no product news for the fifth consecutive scan**, but the **Python `salesforce-cdp-connector` is deprecated** with its replacement `salesforce-datacloud-connector` still at **2.0.0b1 beta** and no GA date — and a Salesforce reference app ([`next-gen-wealth`](https://github.com/salesforce/next-gen-wealth)) added **fail-safe messaging to its Data 360-grounded prompt templates**, which is the standing question to ask of every template you write: *what does this say when its grounding returns nothing?* Two method notes: an **org-wide OSPO CODEOWNERS sweep on July 29** makes many repositories look freshly updated and means nothing technically, and `arxiv.org`/`huggingface.co` both returned **403**, so "no new paper" is well-supported for GitHub artifacts and weaker elsewhere.

**Previous additions (scan of 2026-07-29):** the empty-window streak broke. **`agentforce-adlc` merged five pull requests on the evening of July 28** (21:48–22:50 UTC) — and note the correction this forces on yesterday's entry below: that scan reported "no commits since July 24" and debunked the repo's July 28 timestamp as stale metadata. **That was right when checked and wrong hours later.** A negative finding carries a timestamp. The consequential change is a **removal**: the **`/agentforce-secure` skill is deleted**, folded into `/agentforce-test` as **Mode C**. The old suite hard-coded **57 OWASP LLM Top 10 cases around Salesforce-the-vendor** — pointed at an airline complaint agent it asked about Salesforce security bulletins while never testing rebooking-without-verification. Mode C instead **generates cases from the agent's own Agent Script and inferred business domain**, and flips the default to **simulate-unless-`--live-actions`**, with `C1-run` refusing non-sandbox orgs. Also merged: a hook that **blocks `DELETE` without `WHERE` in quoted SOQL** (the LLM-authored-dynamic-SOQL risk), **hooks now gated to Salesforce projects only** (they previously fired in every repo), an **MCP server registry management** skill, and **eight voice latency anti-patterns plus seven voice-safe action rules** — of which *look up internal IDs rather than asking for them* is the one that decides whether an action is usable by voice at all. On research, **AnchorBench** landed publicly on **July 27** — long-horizon persona stability over **85–130 sessions**, and its findings transfer straight to long-lived service agents: **user-state changes are recalled at chance (~0.25)**, **no memory setting wins** (0.430–0.459), and **emotional vulnerability exposes more failures than explicit attacks**. Its paper is **not on arXiv**; the repo README is the only source. Data 360 was **empty for the fourth consecutive scan** — gap-fill covers the **Data 360 MCP Server** (Developer Preview, ~200 REST operations behind three facade tools), distinct from Headless 360 and one maturity step behind it.

**Previous additions (scan of 2026-07-28):** the 24-hour and 72-hour windows were **both empty across all three areas** — the third consecutive empty 24h window — so this scan again went after **verified gaps**. The largest was a flagship product this radar had never recorded at all: **Agentforce Coworker**, announced **May 21, 2026** and **in Beta for all Agentforce customers** since. It puts an agent in the search bar — Salesforce's and Slack's today, Teams/ChatGPT/Claude later this year — and it **inverts the year's pattern**: you author nothing, so what it can answer is decided entirely by the org's sharing model. Enabling it is an instant org-wide sharing audit. Two things to hold onto: the widely-repeated **"270+ data sources" is marketing** — the Beta docs list **three** (CRM, Data 360 objects, Slack), with Drive/SharePoint/Jira pilot-only — and **seat-based licences search CRM and Slack for zero credits**. Full write-up in [02-salesforce-ai/10-agentforce-coworker](../02-salesforce-ai/10-agentforce-coworker/notes.md). The gap with a live deadline: **July 24, 2026 was the last day to register** for the **24 certifications retiring February 1, 2027**, and the **last day to sit one is August 31, 2026**; separately, **16 certifications were renamed on July 24** with *Agentforce* replacing the old cloud branding (cosmetic — your Trailblazer profile updated itself, your CV and LinkedIn did not). On Data 360, three Summer '26 status changes were missing from the radar: **Accelerated Data Ingest is GA** (real-time CRM data, no pipeline lag — this kills the most common "stale grounding" failure), **AWS Glue federation is GA**, **Microsoft Fabric OneLake federation is only Beta**, plus **SQL from Apex** and IdP auth on the Databricks connector. On the research side, two benchmarks were never captured: **MFCL Audio** (voice function calling, 6.2K tasks, presented at **ICML 2026**, July 6–11, Seoul) and **LoCoBench-Agent** (long-context software engineering, 8,000 scenarios, 10K–1M tokens). Note one debunked signal: `agentforce-adlc` shows "updated July 28" on GitHub, but `main` has **no commits since July 24** and **no releases at all** — repository metadata, not code. _(**Superseded by the 2026-07-29 scan.** This was accurate when checked; five PRs then merged at 21:48–22:50 UTC on July 28, after that scan ran. The stale-metadata reasoning was sound — the conclusion simply expired.)_

**Second pass (2026-07-28, structural):** a follow-up question — *are any Agentforce topics missing?* — produced the largest single correction to this radar's coverage so far, and **not a news item**: the empty windows above were independently re-confirmed. Grepping the whole study base found four parts of the 2026 Agentforce surface that had never been captured. The worst was **MuleSoft Agent Fabric** — **zero mentions repo-wide, radar included**, despite launching **September 2025**. It's the control plane for agents you *didn't* build: **Agent Registry** (federated — registries reference each other), **Omni Gateway** (policy on every A2A and MCP call), **Agent Broker** (graph routing; networks declared in YAML), **Agent Visualizer**. Scanners for **Agentforce, Bedrock, Vertex AI and Copilot Studio** went **GA January 2026**; **guided determinism** landed **April 15, 2026**. The distinction to hold: **Agentforce orchestration coordinates agents inside one org; Agent Fabric coordinates agents across vendors.** Second, **Agentforce Voice and Contact Center** had one passing bullet between them despite **Contact Center being GA since 2026-02-23** — and voice carries three project-deciding facts: **US/Canada only** as of early 2026, **two billing models** (30 credits/action *or* ~60 credits/min, a 3× swing on a five-minute call), and **escalation prerequisites that live outside Agentforce**. Third, the **ADLC** now has a first-party command sequence (`sf agent generate` → `preview` → `test` → `publish` → `activate`) driven by three Agent Skills — and a licence distinction that matters commercially: `sf-skills` is supported, **`agentforce-adlc` is CC BY-NC 4.0 and unusable on client work**. Fourth, **prebuilt agents** (Help Agent, Commerce, IT Service, Missionforce) had no buy-vs-build framework anywhere. Two vocabulary traps recorded: **Flex Gateway was renamed Omni Gateway** (cosmetic, non-breaking), and **"Agent Script" names two different products**. Full write-ups in [02-salesforce-ai](../02-salesforce-ai/INDEX.md) topics **11–14**; the pass itself is documented in [01-agentforce/2026-07-28.md](01-agentforce/2026-07-28.md).

**Earlier additions (scans of 2026-07-27 and 2026-07-26) — condensed at the 08-02 weekly pass, because the substance now lives in topic files.** Agentforce Commerce **GA 2026-07-06** with native ChatGPT selling; **Data 360 pricing rebuilt 2026-03-02** on a profile-based SKU (~$240 / 1,000 profiles) inside the shared Flex Credits pool; **Multi-Agent Orchestration is GA (2026-06-15), not Beta** — a correction, with **Atlas Reasoning Engine 3.0 routing on subagent descriptions**; the **Headless 360 MCP Server** at Beta with ~100 admin-facing skills; `agentforce-adlc` shipping Agent Script lifecycle skills; and the VA's **$1.6B "Agentic Enterprise License Agreement"** (2026-07-24), a third commercial model beside Flex Credits and pay-per-resolution. Full detail: [pricing-and-certification.md](pricing-and-certification.md), [agentforce-platform.md](agentforce-platform.md), [developer-tooling-and-apis.md](developer-tooling-and-apis.md) · scan notes [07-27](01-agentforce/2026-07-27.md), [07-26](01-agentforce/2026-07-26.md).

## Open questions to chase

- **Which line does the 2026-08-26 promotion take `sf` `latest` to — and is the shrinkwrap documented anywhere?** Two things ride on the first half. If `latest` goes to **2.149.9**, stable gains SDR **13.1.1** (the TOCTOU symlink fix, unpatched on stable for 13 days) and `@salesforce/agents` **2.0.4** (all four `--api-name` preview fixes), but **not** the `connected_subagent` publish fix, which needs the 2.150.x line and therefore **2026-09-02**. If it skips to 2.150.x, both land at once. The second half is the one with legs: **no Salesforce documentation of the shrinkwrap was located** — not in the CLI release notes, not in the install docs — so the only way to answer *"is this CLI patched?"* is to unpack the tarball. **Check the dist-tags on 08-26 after 17:00 UTC (Pacific business hours — a pre-dawn UTC check reads the previous day), diff the two shrinkwraps, and look for any first-party statement that the CLI pins its tree.** _(Raised 2026-08-25.)_
- **Does SDR 13.2.x reach a CLI at all, and did anyone get hit by the four-hour `ENOTDIR` window?** No `sf` channel pins above 13.1.1 as of 2026-08-25, so the decomposed-Permission-Set resolution fix is unreachable from the CLI. Separately, **13.2.1 has a changelog entry and no npm artifact** — unexplained, with no issue or postmortem located, and the pattern matters because a failed publish is invisible from the repository side. **Watch for 13.2.x appearing in a `nightly` shrinkwrap, and for any `forcedotcom/source-deploy-retrieve` issue about the 13.2.2 crash or the missing publish.** _(Raised 2026-08-25.)_
- **Two Salesforce AI Research code drops have no papers, and a third harness has never been examined.** The 2026-08-21 gap check wrote up [`self-improve-fragility`](ai-research-and-benchmarks.md#2026-08-11--self-improve-fragility--salesforce-measured-whether-self-improving-agents-improve-and-the-answer-needed-12-orgs) and [ClaimWriter / ClaimProbe](ai-research-and-benchmarks.md#2026-08-05--claimwriter--claimprobe--a-claim-level-faithfulness-audit-released-noncommercial-before-its-paper-exists) from **code and README alone**. Neither paper is locatable: *"On the Fragility of Self-Improving Agents"* has no arXiv id, and ClaimWriter's `CITATION.cff` says *"Authors to be added after publication… under review."* So the radar holds the **method** for both and **no result for either** — which is the honest state, but it means the fragility finding cannot yet be quoted to anyone. **Three things to settle:** the arXiv ids when they appear; whether **`SalesforceAIResearch/WALT`** (the WebArena harness `self-improve-fragility` adapts, absent from this entire study base) is worth its own entry; and four more never-recorded org repos last touched June 2026 — **`MAS-Orchestra`**, **`CaOPD`**, **`vibepass`**, **`gui-process-automation`**. **Watch arXiv for both titles and sweep the four repos on the next gap check.** _(Raised 2026-08-21.)_
- **What is `GoalBasedAgent` / AgentIQ as a *product*, and does it version, deploy and publish like a conversational agent?** The language surface is fully public and Apache-2.0 in `salesforce/agentscript` at 3.4.0 ([entry](agentforce-platform.md#2026-08-19--agent-script-3x-adds-a-second-agent-topology--goalbasedagent-agents-run-on-a-schedule-not-on-a-conversation)); **nothing else is.** No Salesforce announcement, release note or doc page names either term as of 2026-08-20. Unresolved and all consequential: **what identity a cron-triggered run executes as** (the single biggest gap — a scheduled agent has no user turn to inherit sharing and FLS from); whether a GBA compiles to the same `AiAgentDefinition` / `AiAgentDefinitionVersion` v68 metadata pair as a conversational agent or something new; whether `trigger:` schedules are org-visible anywhere in Setup; and whether the eval gate on `publish` applies. **Watch the Winter '27 release notes and the v68 metadata roster for a goal-based type, and re-clone `salesforce/agentscript` each run — this landed with no announcement and the next piece will too.** _(Raised 2026-08-20.)_
- ~~**Is the hosted `headless-360` MCP server still Beta?**~~ **Partly answered 2026-08-20 — it is still labelled Beta**, per the reference page title *"Headless 360 (Beta)"*, so the `semver: ">=1.0.0"` floor found on 08-16 is not a GA signal. **Still open:** what `api.agentic.access.enableHeadless360McpServer` gates and how it is enabled — see the full question below.
- **Does Salesforce intend the `data360-*` skills to depend on an unsupported personal repo, and is a first-party `sf data360` plugin coming?** **Sharpened 2026-08-20:** the 08-19 announcement promises **prebuilt Data 360 Skills at GA "targeted August 2026"** while `forcedotcom/d360-mcp-server` sits untouched since 2026-07-02 and the shipping `data360-*` skills still call the community plugin. **The question is now whether the announced Skills are these skills, a hosted replacement, or a third thing** — and nothing in either source references the other. Original question follows. Written up at [data-360.md](data-360.md#2026-08-17--the-seven-data360--skills-run-on-an-unofficial-community-cli-plugin--and-1390-deleted-the-line-that-said-so). `sf-skills` states the arrangement deliberately — *"sf-skills does not vendor or fork that plugin"* — yet 1.39.0 **removed** the `compatibility:` declaration from all seven skills without replacing it, which is the opposite direction from the `cliTools` / `accessCheck` / `mcpTools` trend of declaring more. Unresolved: whether the removal is deliberate or collateral from the frontmatter reordering; whether `@salesforce/plugin-data-code-extension` (the real, official one) is the beginning of a first-party replacement; and whether the four wrong `@salesforce/plugin-data-codeextension` lines are a known bug. **Open an issue on `forcedotcom/sf-skills` for the misnamed package — it is unambiguous and cheap — and watch whether `compatibility:` returns on the next release.** _(Raised 2026-08-19.)_
- **Is `@salesforce/afv-skills`'s `CC-BY-NC-4.0` manifest a mistake or the intended licence for the npm channel?** **Re-checked 2026-08-19: unchanged on 1.39.0 and 1.40.0**, so it is not a one-release slip — 27 consecutive versions now. The repository is Apache-2.0 (`LICENSE.txt`, relicensed 2026-06-29) and the npm package built from the same tree has declared **NonCommercial on every version published before and after** that date — with an Apache-2.0 `LICENSE.txt` inside its own tarball. Written up at [pricing-and-certification.md](pricing-and-certification.md#2026-08-16--sf-skills-is-apache-20-on-github-and-cc-by-nc-40-on-npm--one-tree-two-licence-statements). Unresolved and material: **which channel Agentforce Vibes auto-installs from** (never stated publicly), and whether Salesforce will correct the manifest or the file. **Open an issue on `forcedotcom/sf-skills` asking which statement governs, and watch the `license` field on the next Friday release.** Until then, take the skills from GitHub, not npm, for anything billable. _(Raised 2026-08-16.)_
- **Is the hosted `headless-360` MCP server still Beta, and what does `api.agentic.access.enableHeadless360McpServer` gate?** 08-16 established the server's *shape* — four meta-tools over raw HTTP, org bound to the session JWT — from first-party consumers in `sf-skills` 1.38.0, which also declare `semver: ">=1.0.0"` and name that org gate for the first time. **A declared floor is not a GA announcement**, and the radar's own record still says Beta from 07-26. Unresolved: the server's current status, whether the gate is a permission, a preference or a feature toggle, how it is enabled, and whether the "~100 admin-facing skills" figure ever described anything real. **Find the gate in Setup on a preview org and check the hosted-MCP docs for a status change.** _(Raised 2026-08-16.)_
- **`sf-pi` ships three breaking changes this radar has never recorded, and none of them can be dated.** The `CHANGELOG` keeps everything under a permanent **`## Unreleased`** heading — it still contains the May 2026 pi 0.74 scope migration — so a reader cannot tell which release carried which change. Three matter. **(1) Agent Script publication is now eval-gated:** publish always creates an **inactive `BotVersion`**, `agentscript_eval action='run_release'` runs the generated baseline plus `tests/agentforce/<AgentApiName>.eval.json`, activation is a separate step, the **`publish activate=true` contract is removed**, and emergency untested activation needs a Guardrail-approved **`acknowledge_untested_activation=true`**. **(2)** The SF Brain replacement kernel is gone, replaced by an immutable **Salesforce Engineering Constitution** — `SF_KERNEL.md` is no longer read, guidance is append-only through `<globalAgentDir>/sf-brain/SF_CONSTITUTION_APPEND.md`, and `SF_REFERENCE_MAP.md` routing is removed. **(3)** The bundled `sf-agentscript` and `sf-browser` Pi skills are **retired** into extension-owned `AGENT_GUIDE.md` files — **dated 2026-08-25: this one shipped in v0.270.0 and v0.271.0, both 2026-08-19** (*"turn AGENT_GUIDE.md into a Pi-style index"*, PRs [#632](https://github.com/salesforce/sf-pi/pull/632) and [#634](https://github.com/salesforce/sf-pi/pull/634)), so (1) and (2) are what remain undated. **Date each against a release tag by bisecting `git log` on `CHANGELOG.md`, then write up (1) — it is a breaking change to the publish flow of the platform's default authoring model.** _(Raised 2026-08-11.)_
- ~~**Are arXiv 2602.17017 and 2606.07462 Salesforce AI Research papers?**~~ **Closed 2026-08-11 — neither is.** *Sales Research Agent and Sales Research Bench* (2602.17017) is **Microsoft Dynamics 365 Sales**; *Act As a Real Researcher* (2606.07462) is **Xi'an Jiaotong University and Xidian University**. Out of scope for this radar. The method note is worth keeping: *"Sales"* in a title is not *"Salesforce"*, and the 08-10 note surfaced both purely on a title match.
- **Two Summer '26 Data 360 features are named in the release notes and absent from this entire study base** — *"Gain Accurate Financial Insights with **Currency Reporting** in Data 360"* and *"Improve Dashboard Responsiveness with **Result Reuse for Data 360 Live**"*. No technical detail is obtainable because `help.salesforce.com` is unreadable to automated fetching. **Open the release notes in a browser and settle both.** _(Raised 2026-08-01; the third item — the no-code semi-join / anti-join builder — was **closed 2026-08-07**: it is a **CRM Analytics lens** feature in **Beta**, doc page `analytics.bi_explorer_data_join.htm`, not Data 360 at GA. The 08-01 caveat was right.)_
- ~~**When is `sf` 2.147.x promoted to `latest`?**~~ **Closed 2026-08-13 — it was promoted on 2026-08-12**, and the answer retires the question's premise. Written up at [developer-tooling-and-apis.md](developer-tooling-and-apis.md#2026-08-12--sf-21477-is-promoted-to-latest--node-22-becomes-the-stable-floor-and-two-waiting-fixes-ride-in-with-it). `latest` **2.147.7**, `latest-rc` **2.148.3**, `nightly` **2.149.0**. ~~Both withheld fixes — the SDR zip-slip patch and employee-agent preview — arrive together, by range resolution rather than by pin.~~ **Corrected 2026-08-25: neither arrived.** 2.147.7's `npm-shrinkwrap.json` pins SDR **13.0.0** and `@salesforce/agents` **2.0.0**; the zip-slip fix reached stable on **2026-08-19** with 2.148.3, and the employee-agent fix with it. The closure of *when* the promotion happened stands; the claim about *what it delivered* does not. **The method lesson is the durable part: there was no stall.** The CLI release notes say stable ships **on Wednesdays**; 2.147.7 published to npm 08-05 and soaked as `latest-rc` for one week, exactly as documented. Five scans tracked a published schedule as if it were a decision. Two traps survive the closure and are recorded in the entry: the release notes **date a version by its planned stable date, not its publish date**, and a lockfile can hold you on the unpatched tree while a fresh install is clean. _(Raised 2026-08-02; re-checked 08-05 and 08-08 through 08-12.)_
- **What exists in which regulated environment — and which body authorized IL5, on what date?** The 2026-08-05 announcement established that **the Einstein Trust Layer's model roster is a property of the environment, not the org** (Anthropic-supplied models had to be disabled to obtain IL5). Two gaps follow. **First, there is no matrix anywhere** for commercial vs Government Cloud vs Government Cloud Plus vs IL5 — which models each offers, which features are excluded (Government Cloud Plus already drops **Agentforce Coworker**, **Vibes** and **ApexGuru/Scale Center**), and whether an IL5 sandbox matches its production peer. **Second, neither press release names the authorizing organization, the authorization date or an ATO package identifier** — so "IL5-authorized" is the company's claim until a DISA listing confirms it. Every source located is a press release or secondary coverage; `salesforce.com`, `meritalk.com` and `defensescoop.com` are all unreachable to automated fetching. **Build the matrix from [compliance.salesforce.com](https://compliance.salesforce.com/) and find the DISA listing.** Also open: whether this warrants its own `02-salesforce-ai` study folder. _(Raised 2026-08-06 and 2026-08-07; merged 2026-08-08.)_
- ~~**`salesforce/sf-pi` ships an `sf-data360` extension this radar has never examined.**~~ **Closed 2026-08-09** — written up at [developer-tooling-and-apis.md](developer-tooling-and-apis.md#2026-08-09--sf-pis-sf-data360-extension--eleven-data-360-tools-and-no-mcp-runtime-in-sight): eleven `data360_*` tools, stable and default-enabled, no MCP runtime. **The follow-on stays open:** `sf-soql`, `sf-data-explorer` and `sf-brain` are still unexamined, and the `sf-guardrail` write-up covers only its command-gate surface. The 08-09 lesson says to read the repo's docs directory before trusting a press-release silence.
- **What else is in Winter '27, and which API version is it?** **Partly answered 2026-08-10.** Two more Release Updates were located by gap check: **Update Instanced URLs in API Traffic** (now written up at [trust-security-and-governance.md](trust-security-and-governance.md#2026-08-10--winter-27s-other-api-break--instanced-urls-in-api-traffic-reach-end-of-support)) and a **base-component accessibility batch** — cards, docked containers, menu lists, panels, date pickers, popovers, utility bars, record headers, modal windows — with no per-component detail obtainable. **Still open:** the full Release Update list, *Custom Lightning Type Bundles with Agentforce Vibes Skills*, and two Data 360 feature titles from secondary coverage with no surface (*select writeback operation types for Data 360 objects*, *assign data model object categories for Data 360 compatibility*). ~~**The API version is still unconfirmed**~~ — **closed 2026-08-13: Winter '27 is API 68.0.** SDR **13.1.0** regenerated `METADATA_SUPPORT.md` with a **`## Next Release (v68)`** section listing **59 new metadata types**, which also gives the new-type roster the release notes never enumerated ([entry](developer-tooling-and-apis.md#2026-08-12--winter-27-is-api-680-confirmed--and-it-brings-a-new-agent-runtime-metadata-pair-aiagentdefinition--aiagentdefinitionversion)). **Two scans declared this unobtainable from this environment and both were wrong** — it needed a generated file in a repo already on the weekly list, not an org endpoint. **Still open:** the full Release Update list, *Custom Lightning Type Bundles with Agentforce Vibes Skills*, and two Data 360 feature titles with no surface (*select writeback operation types for Data 360 objects*, *assign data model object categories for Data 360 compatibility*). Also newly open from the v68 list: **what `AiAgentDefinitionPlanner` is**, and whether the four telephony-provider types are the Agentforce Voice provider surface. **Open Setup → Release Updates in a preview org after Aug 30 and enumerate what is actually queued.** _(Raised 2026-08-09; narrowed 2026-08-10; version closed 2026-08-13.)_
- **Agentforce Operations has no obtainable first-party detail and no price.** GA **2026-04-29**, now recorded in [agentforce-platform.md](agentforce-platform.md#2026-04-29--agentforce-operations--the-fifth-prebuilt-agent-family-ga-for-102-days-and-never-recorded-here) entirely from search-result snippets — every source located, `salesforce.com` included, was blocked to automated fetching. Unresolved: **what a digital blueprint actually is as an artifact** (metadata type? is it deployable? does it version?), whether the Flow integration ever left the **May 2026 Beta**, and **what it costs** — no SKU or Flex Credits mapping exists anywhere, which is the one cell that keeps the buy-vs-build table from closing. _(Raised 2026-08-09.)_
- **"Agent Albert" is absent from this entire study base and this radar has never recorded it.** Coverage from around **April 2026** describes a Salesforce AI platform, code-named Agent Albert, that **observes end-user work and acts on the user's behalf**, targeted for a public unveil **by the end of 2026** — positioned as the successor step to Agentforce rather than a feature of it. Every source located is secondary (`salesforceben.com` and `the-decoder.com` both return **403** to automated fetching), so there is **no first-party detail, no product surface and no confirmed date**. It is not a window item and was deliberately not written up. **Find a first-party source — a Salesforce newsroom post, a Dreamforce '26 session, or an earnings-call transcript — before it appears in anyone's roadmap conversation.** _(Raised 2026-08-05.)_
- ~~**SCUBA has no captured detail anywhere in this study base.**~~ **Closed 2026-08-07** (recovered to `main` 2026-08-08). Written up at [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md#2025-09--scuba--300-real-crm-tasks-and-the-number-that-ends-the-agents-can-just-drive-the-ui-conversation) — [arXiv 2509.26506](https://arxiv.org/abs/2509.26506), September 2025, Apache-2.0.
- ~~**Winter '27 release notes aren't public yet.**~~ **Closed 2026-08-09** — they are live. The prediction held: they were the most substantial source this radar has had in two weeks. Enforcement, dates and the OAuth retirement are written up at [trust-security-and-governance.md](trust-security-and-governance.md#2026-08-09--winter-27-release-notes-are-live--and-the-oauth-username-password-flow-retires-on-your-orgs-upgrade-date). What remains open is *contents*, above.
- **Agentforce Coworker GA** — Beta since May 21, 2026 with **no announced GA date**, and no word on whether the pilot sources (Google Drive, SharePoint, Jira) reach Beta first. Also unconfirmed: the "later this year" ship dates for the Teams, ChatGPT, Claude and desktop surfaces. Re-check before it goes near a delivery commitment.
- **Context Indexing GA** — reported in June 2026 as expected "later in July 2026", but no confirmation found as of 2026-07-28. Status open; re-check against the monthly Data 360 release notes.
- **Agent Broker's real status** — launch coverage puts it **GA October 2025**; coverage of the April 2026 Agent Fabric expansion describes a **Beta from April 2026**. Likely reconciliation: base Broker GA, guided determinism in Beta — but that's inference. Also unresolved: what Agent Fabric costs and which MuleSoft entitlement it needs. **No public pricing found.**
- **The official Agentforce Specialist domain weights** — third-party resources report **Multi-Agent Interoperability at 15%** and a **Development Lifecycle & Observability domain at 15–20%**, against the ~5% and no-such-domain figures this radar recorded on July 26. Sources conflict; the official guide couldn't be read first-party. **If the higher numbers hold, ~30% of the exam sits in two areas the study plan treated as peripheral.** Read the official PDF and settle it — this is the highest-value open question here.
- **Agent Script in the Agentforce Specialist exam** — it became the default authoring model in July 2026 but isn't named in the exam guide. Assume implicit scope; re-check before booking.
- **Exact Agentforce Builder / Agent Script GA date** — no first-party announcement located; secondary sources conflict between February 2026 and the Summer '26 cadence. The July 13, 2026 legacy-builder cutoff is confirmed.
- **`salesforce-datacloud-connector` GA** — the Python Data 360 client v2 has been at **2.0.0b1 beta since June 2, 2026** with no announced GA date, while **v1 (`salesforce-cdp-connector`) is deprecated with removal tied to that GA**. Re-check before writing either into anything long-lived.
- **Fin acquisition** — Salesforce signed a definitive agreement to acquire Fin (announced June 15, 2026; expected to close Q4 FY27), an SMB-focused autonomous service agent platform used by 30,000+ companies. Watch how it lands relative to the Help Agent.

---

## Writing contract

The `daily-agentforce-updates` routine writes to this contract. It lives here rather than in the prompt so the rules travel with the repo.

### Routing — one canonical home per item

Write the full entry in exactly one topic file. Other topic files may carry a one-line cross-link, never a copy.

| Subject | Canonical file |
|---|---|
| Builder, Agent Script, orchestration, Voice, Contact Center, mobile SDK, observability, Coworker, prebuilt agents | [agentforce-platform.md](agentforce-platform.md) |
| Data 360 ingestion, modeling, grounding, SOQL, Code Extensions, connectors, zero-copy, semantic layer | [data-360.md](data-360.md) |
| MCP servers, Headless 360, Agent Fabric, ADLC, `sf` CLI, npm libraries, Apex/LWC, IDEs | [developer-tooling-and-apis.md](developer-tooling-and-apis.md) |
| Trust Layer, execution modes, sharing, auth retirements, secrets, Release Update enforcement | [trust-security-and-governance.md](trust-security-and-governance.md) |
| Flex Credits, pay-per-resolution, ELAs, certifications, acquisitions | [pricing-and-certification.md](pricing-and-certification.md) |
| Benchmarks, papers, open models, evaluation method, research-artifact licences | [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) |

**Tie-breakers.** MCP servers route to `developer-tooling-and-apis.md` even when Data-360-specific — `data-360.md` gets the cross-link. A licence, price or exam-scope fact about a tool routes to `pricing-and-certification.md`; the tool itself stays in its own file. A **licence on a research artifact** stays in `ai-research-and-benchmarks.md`, because there the licence *is* the finding. A CVE-class fix in a Salesforce npm package or CLI routes to `developer-tooling-and-apis.md` — `trust-security-and-governance.md` gets the cross-link and the generalised governance point.

### Topic-file entry — the primary deliverable

```markdown
## YYYY-MM-DD · <Title — names the thing, no marketing verbs>

**What changed.** <the fact, ≤60 words>

- **<Bolded lead label>.** <detail>
- **<Bolded lead label>.** <detail>

**Why it matters.** <2–4 sentences: the practitioner consequence, not a restatement of the fact.>

**Gotchas:**
- <the trap, with the exact identifier that triggers it>

**Study action:** <one concrete thing to do in a dev org, a repo or the CLI.>

**Status:** GA / Beta / Pilot / Developer Preview / Announced / Open source — plus the release and date.

**Sources:** [Title](url) · [Title](url)
```

Rules:

- **No paragraph over ~60 words.** Any inline enumeration — "four skills:", "Three concrete things", "(1)…(2)…(3)" — becomes a list.
- **`**Why it matters.**` is a fixed literal string.** Not "why it's worth your time", not "what it means practically". It has to stay greppable.
- **`**Study action:**` is mandatory on every entry.**
- **`**Gotchas:**` is mandatory when the item has an API, CLI, metadata type, permission set or config surface**, and identifiers must be exact. The bar is the Data Library entry: treat the library as ready when `retrieverId` goes non-null, not when `status` flips; `rag_feature_config_id` is `"ARFPC_" + libraryId`.
- **Mermaid diagrams belong here**, not in a scan log. Use one for a flow, a routing decision or a dependency graph.
- **A negative finding carries a timestamp.** "No commits since July 24" was true when checked on 07-28 and false hours later.

### Write discipline

- **New entry** → append at the top. Every topic file reads newest-first.
- **Status change to an existing entry** → mutate in place and prepend `> **Correction (YYYY-MM-DD):** <what this said before, what it is now>`.
- **Never bump an existing heading's date.** That is what put a 07-27 entry between two 07-26 entries in `agentforce-platform.md`.
- **Never delete a superseded claim** — supersede it visibly.
- Touching several topic files in one run is normal; top-appending in all of them at once is what caused the merge conflict in `bbff18a`, so commit once per run.

### Dated scan note — an audit log, not a write-up

Target ≤25 lines. No table of contents. No `## What this scan did not find` essay. No `**Status:** N/A — scan record`.

```markdown
# <Area> — <Month D, YYYY>

**Window:** <UTC range> (24h / extended to 72h) · **Checked:** <sources>

## What changed
- **<Item>** → [agentforce-platform.md](../agentforce-platform.md#anchor) — <one line>

## Verified negatives
- `salesforce/agentscript` — newest `main` commit 2026-07-24, "<subject>", checked <UTC time>
```

A verified negative names the artifact and when it was checked. Anything vaguer is omitted, not padded.

### Standing notes — do not restate these in dated notes

- `salesforce.com`, `salesforceben.com`, `arxiv.org` and `huggingface.co` return **403** to automated fetching. Use search-result snippets and secondary coverage; a negative from these sources is weaker than one from GitHub.
- **Egress varies between runs and can be much narrower than 403s alone.** On 2026-08-09 `help.salesforce.com`, `developer.salesforce.com`, `admin.salesforce.com`, `api.github.com` and every secondary blog located returned **EGRESS_BLOCKED** — a proxy refusal, not a site 403. GitHub was readable only via page fetches and npm via the registry. When this happens, say which sources were reachable in the dated note and treat everything else as unchecked, not as absent.
- An **org-wide OSPO CODEOWNERS sweep on 2026-07-29** makes many Salesforce repositories look freshly updated. Repository metadata is not code — check commits.
- The three `agentforce-*` skills ship from one internal source into two public repos at identical versions: `SalesforceAIResearch/agentforce-adlc` (**CC BY-NC 4.0**, blocks client work) and `forcedotcom/sf-skills` (**Apache-2.0** per `LICENSE.txt`). The restriction attaches to the copy you took — prefer `sf-skills` for anything commercial, **and take it from GitHub**: as of 2026-08-16 the npm package `@salesforce/afv-skills` declares `CC-BY-NC-4.0` in its manifest on every version, contradicting the Apache-2.0 `LICENSE.txt` in its own tarball. The unit is the **channel**, not the repository.
- **`forcedotcom/sf-skills` is a primary product source, not a tooling repo.** Salesforce ships capability documentation — licence gates, feature keys, permission-set API names, enablement order — as skill frontmatter and prose *for an agent to execute*, sometimes with no press release, release-note entry or reachable doc page. Do not infer absence from silence elsewhere. **The Friday cadence ended in August 2026** — 1.38.0 (Fri 08-14), 1.39.0 (Mon 08-17), 1.40.0 (Tue 08-18) — so check npm `@salesforce/afv-skills` every run, not weekly. And **the changelog is not the release**: 1.39.0's one-line "Release 25 skills updated" carried a deleted prerequisite declaration and a regenerated capability catalogue. Read the frontmatter and `references/`.
- **Provenance does not propagate from the repository to the runtime.** An Apache-2.0 skill published by `forcedotcom` may call a tool Salesforce neither ships nor supports — the `data360-*` family runs on a 3-star personal repo. Before treating a skill path as first-party, follow the command it emits to the thing that executes it.

### Feeding the study base

- New jargon defined in an entry → the right alphabetical section of [GLOSSARY.md](../GLOSSARY.md).
- Each scan adds strict `Q:` / `A:` pairs to the relevant `../02-salesforce-ai/NN-topic/flashcards.md`.
- An item big enough to be its own subject (a product or capability with its own surface, not a version bump) → next-numbered `NN-kebab-case` folder in [02-salesforce-ai](../02-salesforce-ai/INDEX.md) from `../_templates/`, update that INDEX, and link it from the radar entry with a `**Study folder:**` line.

### Coverage, staleness and the weekly pass

- **Every run** reports the last-touched date of all **six** topic files. Anything untouched **>14 days** gets a gap check that run. `trust-security-and-governance.md` went 8 commits with zero updates before anything noticed.
- **Weekly (first run on or after Sunday):** rewrite *State of play*, prune resolved *Open questions* and add new ones, refresh the areas-table `Latest` column and the footer date, and reconcile any item that has landed in two topic files.

## Coverage and staleness — 2026-08-19

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [data-360.md](data-360.md) | 2026-08-14 | 5d | ✅ **one entry** — the seven `data360-*` skills run on an **unofficial community CLI plugin**, and 1.39.0 deleted the `compatibility:` line that said so. Ends **eight scans** with no Data 360 entry |
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-17 | 2d | ✅ **two entries** — `@salesforce/agents` **2.0.3 / 2.0.4** (fourth `--api-name` defect in nine days), and the **capability catalogue frozen six releases back** plus `metadata.domains` |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-17 | 2d | ✅ one-line cross-link — provenance does not propagate, and a stale generated catalogue pins stale installs |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-17 | 2d | ❌ nothing in the window — no Builder, Agent Script, Voice, Coworker or mobile-SDK change; `react-native-agentforce` unchanged at 0.5.0 |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-16 | 3d | ❌ nothing in the window — but the licence question was **re-checked**: `@salesforce/afv-skills` **1.39.0 and 1.40.0 both still declare `CC-BY-NC-4.0`**, so the split is not a one-release slip |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-08 | **11d** | ❌ nothing cleared the bar — `agentforce-adlc` **20 days idle**, `agentscript` 16, gift-eval movement is two open third-party submissions. Compulsory **2026-08-22** |

**No topic file exceeded the 14-day threshold**, so no gap check was compulsory. **All three entries came from one shallow clone of `forcedotcom/sf-skills`** — a repository this radar has read weekly for six weeks and has four prior entries about. None of the three is in the changelog: 1.39.0's whole public description is *"Release 25 skills updated"*, and under it sit a deleted prerequisite declaration, a regenerated capability catalogue and a fortnight of stale install pins. **The 08-17 lesson repeated with a different shape.** That scan concluded *"reading a source for the question you arrived with is not the same as reading it"*; this one adds the corollary — **read the artifact, not its summary of itself.** The `compatibility:` removal is invisible in the changelog, invisible in the diff summary (it reads as frontmatter reordering), and only visible by fetching the same file at two tags.

**Next to watch:** whether `sf` **2.148.3** actually reaches `latest` — its own release notes date it **August 19, 2026** and at 03:47 UTC that day it had not moved. If Wednesday promotion has slipped, the 08-13 finding that the cadence is *published, not decided* needs qualifying.

**Method finding — coverage is not the same as re-reading, and this is the second distinct form of that failure in four days.** On 08-14 the lesson was that `mcpTools` "was missed by a habit" — counting skills instead of diffing structure. This run's version is narrower and harder to defend against: a source read **for a specific question** answers that question and gets closed. The CLI release notes were opened to settle a dist-tag timing argument, and the four bullets under the version heading went unread. **When a source is opened to answer a question, enumerate it before closing it.** The cheapest control is a habit: read the whole top section of `releasenotes/README.md`, not the paragraph that matches the query.

**Next to watch:** **2026-08-19** is the concrete one — 2.148.3 is scheduled for `latest` that day, which lands the password break, `--skip-assignment-rules` and the `api request` GA on the channel most teams install from, and would also promote `plugin-deploy-retrieve` **4.1.2** (making `--root-type-with-dependencies` usable off `nightly`). Confirm the promotion actually happened rather than assuming the cadence held. Then **2026-08-21**, the `sf-skills` Friday release, with the three specifics carried over from 08-16: whether the `license` field changes, whether `headless-360` spreads beyond the ITSM cluster, and whether any `data360-*` server appears in `mcpTools`.

---

## Coverage and staleness — 2026-08-16

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-14 | 2d | ✅ **three entries** — the **ITSM CMDB six-skill setup path and its five-layer gate**, **`headless-360` as a four-meta-tool dispatcher**, and **`@salesforce/agents` 2.0.2** |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-02 | **14d** | ✅ **compulsory gap check ran and paid** — new entry on the Apache-2.0-vs-CC-BY-NC-4.0 channel split, plus a `> **Correction**` on the 08-01 dual-licence entry |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-14 | 2d | ✅ two one-line cross-links — the prebuilt IT Service licence gate, and the `--api-name` preview defect |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-14 | 2d | ✅ one-line cross-link — the overloaded `403`, and a dispatcher that carries no org identifier |
| [data-360.md](data-360.md) | 2026-08-12 | 4d | ❌ nothing in the window — **sixth consecutive scan with no Data 360 movement**; no `data360-*` MCP server in any `mcpTools` block across 138 skills |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-07 | 9d | ❌ nothing cleared the bar — `agentforce-adlc` **17 days idle**, gift-eval movement is third-party submissions |

**The compulsory gap check fired and produced the run's most durable finding.** `pricing-and-certification.md` reached exactly 14 days, and five prior scans had deferred it on the reasoning that both its open questions were *unobtainable from this environment* — which was true, and which is why the deferral kept looking correct. The gap check found something neither question asked about: **the licence on the artifact the file's own entry recommends**. Chasing a stale file's *open questions* is not the same as re-examining its *claims*.

**Method finding — the sourcing lesson inverts one more time, and this is its sharpest form yet.** Six scans running, the lead has come from a repository rather than an announcement. This run adds that **the repository was not merely the earliest source but the only one** — a Service Cloud ITSM capability with a licence gate, two feature keys, four permission sets and a six-layer enablement order has no press release, no release-note entry and no doc page reachable from here. It exists publicly as **frontmatter and prose inside skills written for an AI agent to execute**. Salesforce is now documenting products *to agents* before, or instead of, documenting them to people — which makes `sf-skills` a primary product source, not a tooling repo.

**Next to watch:** the **Friday release** of `sf-skills` is now the single highest-yield check on the weekly list — 1.38.0 alone produced two entries and answered a standing watch item. Three specifics for 2026-08-21: whether the `license` field in `package.json` changes; whether `headless-360` spreads beyond the ITSM cluster; and whether any `data360-*` server finally appears in `mcpTools`. Separately, `@salesforce/plugin-deploy-retrieve` **4.1.x** and `plugin-agent` **2.0.2** are both queued behind `nightly` 2.149.x — **2026-08-19** is the earliest either can reach `latest-rc` under the Wednesday cadence.

---

## Coverage and staleness — 2026-08-14

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-13 | 1d | ✅ **three entries** — the **SDR 13.1.1 TOCTOU symlink fix**, **`mcpTools`** in `sf-skills` 1.37.0, and **`--root-type-with-dependencies`** |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-13 | 1d | ✅ one-line cross-link — the working tree as attack surface, and the pin-vs-range propagation rule |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-13 | 1d | ✅ one-line cross-link — retrieving a whole agent's dependency graph in one flag |
| [data-360.md](data-360.md) | 2026-08-12 | 2d | ❌ nothing in the window — **fifth consecutive scan with no Data 360 movement** |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-07 | 7d | ❌ nothing cleared the bar — `agentforce-adlc` 15 days idle, gift-eval movement is third-party submissions |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-02 | **12d** | ❌ not re-checked — both open questions remain unobtainable from this environment. **Compulsory 2026-08-16** |

**No topic file exceeded the 14-day threshold**, so no gap check was compulsory. Two of the three entries are gap-check finds anyway, and they fail in the two distinct ways worth naming. **`--root-type-with-dependencies` published at 2026-08-12 22:15 UTC and the 08-13 scan missed it** — that scan read `@salesforce/cli` dist-tags and SDR, but not `plugin-deploy-retrieve`, so the CLI half of its own lead item went unrecorded. **`mcpTools` was never missed by a window; it was missed by a habit.** `sf-skills` has been covered six times, always by counting skills and diffing frontmatter fields — and a fourth structural field slipped through because this release only *reformatted* it.

**Method finding — the pinning asymmetry is the durable lesson, and it corrects yesterday's framing.** The 08-13 entry concluded that *"range resolution, not the pin, is what delivers the fixes."* That is true one layer down and false at the top: **`sf` pins each plugin to an exact version, and each plugin ranges its libraries.** So a library security patch (SDR 13.1.1) reaches stable `sf` on a fresh install, while a plugin feature (PDR 4.1.0) cannot arrive until a new `sf` ships. Both facts came from the same `package.json` fields on the same day. **Ask which layer a change lives in before predicting whether it can reach you.**

**Next to watch:** `@salesforce/plugin-deploy-retrieve` **4.1.x** is queued behind `nightly` 2.149.x. Per the Wednesday cadence, **2026-08-19** is the earliest it can reach `latest-rc` and **2026-08-26** the earliest for `latest` — the point at which `--root-type-with-dependencies` becomes usable without a nightly build. Also worth a check: whether any `data360-*` or agent-specific MCP server appears in `mcpTools` frontmatter, which would name servers ahead of any announcement.

---

## Coverage and staleness — 2026-08-13

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-12 | 1d | ✅ **two entries** — the **`sf` 2.147.7 promotion to `latest`** and **Winter '27 = API 68.0 + the `AiAgentDefinition` pair** — plus a `> **Correction**` on the 08-11 Claude Code plugin entry |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-12 | 1d | ✅ one-line cross-link — the three-layer agent metadata story, and `AiAgentDefinitionVersion` as a peer of `Bot` |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-10 | 3d | ✅ one-line cross-link — Winter '27 confirmed as 68.0, three v68 security types undeployable, zip-slip fix reaches stable |
| [data-360.md](data-360.md) | 2026-08-12 | 1d | ❌ nothing in the window — **fourth consecutive scan with no Data 360 movement**; no Data-360-specific type appears in the 59 new v68 types |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-07 | 6d | ❌ nothing cleared the bar — `agentforce-adlc` 14 days idle, gift-eval movement is two *open* third-party submissions |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-02 | **11d** | ❌ not re-checked — both its open questions remain unobtainable from this environment. **Compulsory 2026-08-16** |

**No topic file exceeded the 14-day threshold**, so no gap check was compulsory. The run produced the more useful kind anyway: **a fact two prior scans had formally written off as unobtainable.** On 08-09 and again on 08-10 the Winter '27 API version was recorded as unanswerable from this environment, because `login.salesforce.com/services/data/` was blocked at the proxy. It was answerable from `METADATA_SUPPORT.md` in `forcedotcom/source-deploy-retrieve` — a repo on the weekly list — the whole time. **"Unobtainable" was a statement about one method, not about the fact.**

**Method finding — the sourcing lesson has now fired four scans running, and this run adds its inverse.** The last three found their lead in a repository rather than an announcement; this one found a lead in a repository *and* found that a repository's own prose had been answering an open question in plain English. The CLI release notes state the Wednesday stable cadence in their second paragraph, above every version heading. Five scans read the version headings and not the paragraph. **When a schedule looks like a stall, check whether the project has published its cadence.**

**Next to watch:** the **v68 unsupported list** is now a standing source of near-term entries — 37 named types Salesforce is shipping that DX cannot move, including the whole telephony-provider cluster and `AiAgentDefinitionPlanner`. Each SDR minor release that flips one to ✅ is a real capability change with an exact identifier. Watch `METADATA_SUPPORT.md`, not the release notes.

---

## Coverage and staleness — 2026-08-12

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-11 | 1d | ✅ **two entries** — the **`salesforce-development` Claude Code plugin 1.10.0** and the **Data 360 v2 dispatcher (ADR 0106 + ADR 0027)** — plus `> **Correction**` blockquotes on the 08-11 `@salesforce/agents` entry and the 08-09 `sf-data360` entry |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-11 | 1d | ✅ one-line cross-link — the ADLC ships as four subagents plus an `.agent` validator hook |
| [data-360.md](data-360.md) | 2026-08-11 | 1d | ✅ one-line cross-link — `data360_*` is a dispatcher over `registry/v2/actions.json`, with the two destructive env gates named |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-10 | 2d | ❌ nothing in the window; Winter '27 dates re-checked against secondary sources and unchanged |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-07 | 5d | ❌ nothing cleared the bar — `agentforce-adlc` 13 days idle, gift-eval movement is a third-party submission |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-02 | **10d** | ❌ not re-checked — both its open questions remain unobtainable from this environment. **Compulsory 2026-08-16** |

**No topic file exceeded the 14-day threshold**, so no gap check was compulsory — but the run produced one anyway, and it is the more useful kind: **a gap inside something the radar was already reading.** `forcedotcom/sf-skills` has been covered four times (1.33.0, 1.34.0, 1.35.0, and the dual-licence note), always as a *catalogue of skills*. It has also been shipping a **versioned Claude Code plugin with agents, hooks, slash commands and a bundled language-server MCP host** since 1.34.0, and none of those entries noticed. The counting habit — skills added, frontmatter fields changed — read the diff and missed the artifact.

**Method finding:** `raw.githubusercontent.com` was reachable this run and did the heavy lifting — the plugin manifest, both ADRs and the `sf-pi` CHANGELOG were read verbatim rather than summarised from a rendered page. That matters because rendered GitHub views failed again: `github.com/.../compare/1.35.0...1.36.0` timed out, the `.patch` endpoint returned **403**, and `api.github.com` remains scoped to this session's own repository. **When a GitHub fact matters, fetch the raw file path, not the page.**

**Next to watch:** `plugins/builder/` in `sf-skills` is a namespaced directory holding exactly one plugin today. If Salesforce publishes a second, the marketplace becomes a distribution channel rather than a wrapper — worth a weekly check on `.claude-plugin/marketplace.json`.

---

## Coverage and staleness — 2026-08-20

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-17 | 3d | ✅ **two entries** — Agent Script 3.x / `GoalBasedAgent` (with a topology diagram) and voice modality v2 |
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-19 | 1d | ✅ one entry (Headless 360 expansion) + **two corrections** — `sf` `latest` → 2.148.3, and `agents` 2.0.5 is not a fifth defect |
| [data-360.md](data-360.md) | 2026-08-19 | 1d | ✅ one entry (Code Extension plugin 1.4.x) + a one-line cross-link to the announcement |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-19 | 1d | ❌ nothing in the window; the `org generate password` promotion is a status change on its canonical entry, cross-linked from here already |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-16 | 4d | ❌ nothing in the window; no licence, SKU or exam change surfaced |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-08 | **12d** | ❌ nothing cleared the bar — **seventeenth consecutive scan with no AI Research item** |

**No topic file exceeded the 14-day threshold**, so no gap check was compulsory and none ran — the window itself was the richest in a fortnight and absorbed the run. **`ai-research-and-benchmarks.md` becomes compulsory on 2026-08-22**; `gift-eval` has been quiet 7 days and `agentforce-adlc` 22 days, so plan that check around papers and model releases rather than around those two repositories.

**Next to watch:** **2026-08-21**, the `sf-skills` release — with the three specifics carried from 08-16 (whether the npm `license` field changes, whether `headless-360` spreads beyond ITSM, whether any `data360-*` server appears in `mcpTools`) now joined by a fourth: **whether the announced "prebuilt Data 360 Skills" appear there at all**, which would settle whether the GA claim describes these skills or a hosted replacement. Then **2026-08-26**, the next Wednesday promotion, which would put `plugin-agent` 2.0.4's compelling `^2.0.4` floor on stable. And re-clone `salesforce/agentscript` every run — it was quiet for 17 days and then shipped three majors unannounced.

**Method findings, two.** First, **the timezone trap**: this scan runs at ~03:40 UTC, which is the previous *evening* in Pacific time, and npm dist-tags move on Pacific business hours. Yesterday's "2.148.3 missed its own stated date" was that artifact and nothing else. **A same-day negative about a Pacific-scheduled event is a next-day question, not a finding.** Second, **`api.github.com` is gated to this session's own repository**, but **`git clone --depth N` over HTTPS works for any public repo** — which is how every repository fact in this run was obtained, including the three that beat the press release for detail. Prefer the clone to the API. `www.salesforce.com`, `developer.salesforce.com` and `siliconangle.com` were all **EGRESS_BLOCKED**.

## Coverage and staleness — 2026-08-21

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-07 | **13d** | ✅ **three entries** from the gap check — SCUBA's `singleaccess` concurrency fix, `self-improve-fragility`, ClaimWriter / ClaimProbe |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-20 | 1d | ✅ **one correction** — React Native bridge 0.6.0 closes the RC-on-`latest` window opened 08-13 |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-19 | 2d | ✅ one cross-link — `/services/oauth2/singleaccess` is single-use and collides under concurrency |
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-20 | 1d | ❌ nothing cleared the bar; `sf` `latest-rc` moved to 2.149.9 with unreadable release notes, noted as watched |
| [data-360.md](data-360.md) | 2026-08-20 | 1d | ❌ nothing in the window — `d360-mcp-server` 50 days quiet, the connector 80 days in beta |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-16 | 5d | ❌ no licence, SKU or exam change surfaced |

**The gap check ran a day early and was worth it.** `ai-research-and-benchmarks.md` was **13 days** old, one short of the compulsory threshold, and the 24h window held a single correction — so the run went to the gap check rather than padding. It returned **three entries, two of them repositories the radar had never seen**, and it found them the same way 07-28 found Agentforce Coworker: by listing what exists rather than by watching what was already on the list. **Next compulsory: `pricing-and-certification.md` on 2026-08-30.**

**Method finding — the repository list is a source, and sorting it by update date is the query.** Every scan since 07-26 has checked *named* repositories: `agentscript`, `sf-skills`, `gift-eval`, `CRMArena`, `agentforce-adlc`. All five were correctly reported quiet again today. But `github.com/orgs/SalesforceAIResearch/repositories?sort=updated` returns **`SCUBA` (Aug 18), `self-improve-fragility` (Aug 11), `claimwriter-deep-research` (Aug 5)** in its first four rows — three artifacts, none of them on the watch list, two never recorded anywhere in the study base. **A watch list can only report on what is already in it**, and eighteen scans of "no AI Research item" was partly a statement about the list, not about the organisation. Add the sorted org listing to the weekly sweep.

**Second finding — a merge date is not an authorship date.** SCUBA's fix carries a `main` date of **2026-08-18** and was written on **2026-05-07**, sitting 103 days on branch `frontdoor-access-fix`. This radar dates items by when they became reachable, which is right, but "the repo has been quiet since May" was true of `main` and false of the project.

**Next to watch:** the **`sf-skills` release**, still not landed — 1.40.0 has held since 08-18 17:18 UTC, so the Friday cadence's replacement is not yet legible, and the four carried specifics stand (npm `license` field, `headless-360` spread beyond ITSM, any `data360-*` server in `mcpTools`, and whether the announced *"prebuilt Data 360 Skills"* appear there at all). Then **2026-08-26**, the Wednesday promotion that would take `sf` `latest` to the 2.149.x line — contents currently unknown, because the CLI's GitHub release notes fail to render. And keep re-cloning `salesforce/agentscript` every run.

## Coverage and staleness — 2026-08-22

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-20 | 2d | ✅ **three entries and a correction** — the ITSM four-track program, `headless-360` 4 → 14 skills, the v68 roster growth |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-21 | 1d | ✅ one cross-link — IT Service gains Teams, Swarming and UEL tracks |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-21 | 1d | ✅ one cross-link — the Go-toggle write rule and the `TenantSecurity*` types |
| [data-360.md](data-360.md) | 2026-08-20 | 2d | ✅ one cross-link — the promised prebuilt Data 360 Skills still have no artifact |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-21 | 1d | ❌ nothing in the window; the org-wide sweep was unavailable (403) |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-16 | 6d | ❌ no licence, SKU or exam change surfaced |

No topic file is over 14 days old, so **no gap check was compulsory**. **Next compulsory: `pricing-and-certification.md` on 2026-08-30.**

**Method finding — the release was the whole run, and it was worth reading past the changelog line.** `sf-skills` **1.41.0** landed 2026-08-21 14:48 UTC with the one-line summary *"Release 26 new + 11 updated skills"*. Everything of substance came from diffing the two npm tarballs and reading frontmatter and `references/`: the four-track orchestrator, the `headless-360` count going 4 → 14, `ITSMTeamsEnabled` being unwritable by design, the `MsTeamsAppApiFamily` dead end. **Two of the three standing "next to watch" specifics were answered by that diff** — `headless-360` did spread beyond ITSM (one skill, `experience-portal-create`), and no `data360-*` server appeared in `mcpTools`. The third — the npm `license` field — is unchanged at `CC-BY-NC-4.0` for a 28th version.

**Second finding — a nightly-regenerated file needs a timestamp, not a citation.** The v68 metadata roster this radar recorded as "59 types" on 08-13 reads **71** today, because `METADATA_SUPPORT.md` is rewritten by a bot each night. The same file's newest commit (`ac2a8fe`, 08-21 22:07 UTC) is a 915-line Prettier reflow that changes nothing, so its `git log` is loud where its content is quiet and quiet where it is loud. Diff the type names.

**Next to watch:** **2026-08-26**, the Wednesday promotion taking `sf` `latest` to the 2.149.x line — contents still unknown because the CLI's GitHub release notes fail to render. Whether `sf-skills` commit `2476476` (unreleased since 08-21 21:14 UTC) ships as 1.42.0 and whether `compatibility:` returns with it. Whether **August ends without the announced prebuilt Data 360 Skills** — nine days left, and `d360-mcp-server` is 51 days quiet. And `TenantSecurityPlaybookDef`: search the Winter '27 release notes and a preview org's Setup for any surface at all.

---

## Coverage and staleness — 2026-08-25 (weekly consolidation run)

| Topic file | Last touched *before* this run | Age | Touched |
|---|---|---|---|
| [developer-tooling-and-apis.md](developer-tooling-and-apis.md) | 2026-08-22 | 3d | ✅ **four entries and three corrections** — the `sf` shrinkwrap, the `connected_subagent` publish crash, SDR's three-in-one-day, `sf-pi` hiding the skill library |
| [agentforce-platform.md](agentforce-platform.md) | 2026-08-22 | 3d | ✅ one cross-link — multi-agent bundles were unpublishable from stable `sf` |
| [trust-security-and-governance.md](trust-security-and-governance.md) | 2026-08-22 | 3d | ✅ one cross-link — CLI patch state is a lockfile property, not an install-date one |
| [data-360.md](data-360.md) | 2026-08-22 | 3d | ❌ nothing cleared the bar; the `data360-*` skills' new `manual-only` default is recorded once, in developer tooling |
| [ai-research-and-benchmarks.md](ai-research-and-benchmarks.md) | 2026-08-21 | 4d | ❌ no repository in the org moved inside the window |
| [pricing-and-certification.md](pricing-and-certification.md) | 2026-08-16 | 9d | ❌ no licence, SKU or exam change surfaced |

No topic file is over 14 days old, so **no gap check was compulsory**. **Next compulsory: `pricing-and-certification.md` on 2026-08-30.**

**Method finding — the finding was in a file this radar had downloaded four times and never opened.** Every reachability claim since 08-02 was built by reading `dependencies` from the npm registry and doing caret arithmetic. The answer was one file further in: `@salesforce/cli` publishes an `npm-shrinkwrap.json`, and it overrides all of it. Three entries were wrong for three weeks, including a security one — *"the TOCTOU fix is on `latest` today"* was never true. **When a package can pin its own tree, the manifest is a statement of intent and the lockfile is the fact.**

**Second finding — a 72h window with no scans behind it is where day-of churn hides.** SDR cut three releases on 2026-08-24 and the middle one sat on npm `latest` for **4h25m** with a crash in it. A daily scan at 03:36 UTC would have seen none of that; only the changelog and the registry's `time` map preserve it, and they disagree — 13.2.1 exists in one and not the other.

**Next to watch:** **2026-08-26**, the Wednesday promotion — and now the question is precise: does `latest` go to 2.149.9 (SDR 13.1.1, `agents` 2.0.4) or straight to the 2.150.x line (`agents` 2.0.6)? That decides whether the `connected_subagent` publish fix reaches stable next week or on **2026-09-02**. Whether `sf-skills` commit `2476476` (unreleased since 08-21 21:14 UTC) ships as 1.42.0 and whether `compatibility:` returns. Whether **August ends without the announced prebuilt Data 360 Skills** — six days left, `d360-mcp-server` 54 days quiet. And whether SDR 13.2.x reaches any CLI channel at all.

---

_Last updated: 2026-08-25 · Sources are linked inline in each topic file._
